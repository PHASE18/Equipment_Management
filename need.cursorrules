# ====================== 全局项目基础约束 ======================
# 项目类型：设备管理后台系统，前后端分离 SpringBoot3 + Vue3 + TypeScript
## 项目定位
B/S 设备全生命周期管理平台，核心围绕设备档案做全流程管控，支持档案、生命周期、维修、IP、项目、统计、RBAC权限、审计日志、Excel导入、MinIO附件存储。
## 固定技术栈
### 前端
Vue3 + TypeScript + Element Plus + Pinia + Axios + ECharts
### 后端
Spring Boot + JWT鉴权 + MyBatis Plus + OpenJDK21 LTS
### 存储
MySQL8.0（业务库）+ MinIO（附件文件，禁止二进制入库）
### 测试工具
后端单元测试：JUnit5 + Mockito + H2内存库
前端E2E测试：Playwright
### 浏览器兼容
Chrome、Edge最新版，不兼容IE/低版本浏览器

# 二、全局架构强制规范（不可违反）
## 2.1 三层分离架构
1. 前端仅通过REST API（HTTP/HTTPS）与后端通信，禁止前端直连数据库、硬编码业务逻辑
2. 后端分层固定：Controller → Service → Mapper → MySQL，职责严格隔离
3. 统一鉴权方案：JWT，禁用Session、Cookie会话；所有业务接口强制鉴权拦截
4. 附件统一MinIO存储，数据库仅存文件元数据、访问路径；开发环境本地文件仅临时调试，生产必须MinIO

# 三、数据库设计规范（遵循第三范式）
1. 禁止 select *，明确查询所需字段
2. 设备主表必建唯一索引 device_sn，普通索引 gateway_id、online_status、create_time
3. 分页必须强制携带 pageNum、pageSize，禁止无分页全表查询
4. 所有表通用字段统一携带：id、device_sn、online_status、deleted、create_by、create_time、update_by、update_time
5. 每张表、每个字段必须添加 COMMENT 注释
6. 告警历史、设备日志大表按月分表，代码预留分表处理逻辑
7. 禁止在代码中拼接字符串SQL，统一使用MybatisPlus条件构造器

# 四、设备生命周期业务硬约束
标准流转链路：采购中 → 库存 → 待上架 → 在用
分支流转：
1. 在用 → 维修中 → 修复转回在用
2. 在用 → 备用
最终链路：在用/备用 → 停用 → 报废
强制规则：
1. 非法状态转换后端直接拦截返回错误，前端无法绕过校验
2. 任意状态变更自动写入生命周期日志，不可手动删除、不可跳过
3. 设备删除统一逻辑删除 del_flag=1，完整保留历史审计数据

# 五、RBAC三角色权限规范（数据隔离红线）
## 角色权限边界
1. 系统管理员（最高权限）
   - 用户/部门/角色权限配置、全部门设备读写、全部日志查看、批量导入导出、品牌/设备类型基础字典维护
2. 部门管理员
   - 仅可操作本部门设备，禁止跨部门查询/修改/导出；仅查看本部门统计、本部门日志
3. 普通设备责任人
   - 仅查看本人负责设备、修改部分基础信息；仅新增自有设备维修记录；无设备删除权限、无跨部门访问权限
## 权限开发规则
1. 双重校验：功能权限注解 + 数据权限SQL自动过滤；前端隐藏按钮仅交互优化，接口层二次防越权
2. 数据权限自动拼接部门ID/责任人ID过滤条件，禁止前端传参控制数据范围
3. 最小权限原则，不授予超范围操作接口
4. 错误码统一：200 成功、401 未登录、403 无权限、400 参数错误、500 业务异常

七、单元测试强制规范（后端核心业务必须编写）
7.1 技术栈
JUnit5 + Mockito + H2 内存数据库（单元测试不依赖本地 MySQL）
7.2 强制覆盖范围
Service 层核心业务方法 100% 覆盖：设备新增、状态流转、IP/SN 唯一性校验、批量导入、数据权限过滤
工具类：Excel 解析、MinIO 文件上传、JWT 工具、设备状态转换工具
枚举校验、参数校验逻辑
7.3 编码规则
每个 Service 实现类对应独立测试类 DeviceServiceImplTest
必须覆盖正常流程 + 异常边界场景：重复 SN、非法状态转换、越权查询、Excel 错误数据导入、空参数
使用 @AfterEach 自动回滚清理测试数据，不污染测试库
完整断言：返回数据、状态码、异常信息、数据库记录条数全部校验
禁止行为：直接连接开发 MySQL 做单元测试；仅测正常流程忽略异常；硬编码业务 ID、测试账号
7.4 质量要求
核心业务代码单元测试覆盖率≥85%，上线前单元测试通过率 100%，未编写单测禁止提交核心业务代码

八、端到端 E2E 自动化测试规范（Playwright）
8.1 基础规则
页面定位仅使用 data-testid 属性，禁止依赖 CSS class、标签、不稳定文本定位
所有接口请求统一 mock，禁止直接调用开发环境真实业务接口
每条用例执行前自动初始化干净测试数据，执行后清理脏数据
分三类测试账号执行：系统管理员、部门管理员、普通设备责任人，分别校验权限隔离
8.2 必须覆盖全业务场景
登录认证流程：正常登录、密码错误、Token 过期自动跳转登录页
设备完整生命周期全链路：新增设备→入库→待上架→在用→维修→备用→停用→报废
Excel 批量导入场景：正常导入、重复 SN 报错、空文件、错误模板拦截
权限隔离校验：普通用户无法查看他人设备、部门管理员看不到其他部门数据
IP 地址唯一性重复校验弹窗拦截逻辑
附件上传、预览、下载、删除完整流程
首页统计图表加载、统计报表导出
审计日志分页查询、日志导出、日志不可删除校验
8.3 CI 集成要求
代码提交自动执行 E2E 测试，用例不通过阻断合并提交

九、安全开发强制规范
所有业务接口全量 JWT 鉴权，无裸接口
三层校验：前端基础校验 + 后端 DTO 注解校验 + Service 业务逻辑校验
防 SQL 注入：统一 MyBatis 参数绑定，禁止拼接原生 SQL
文件上传强校验：限制后缀（jpg/png/pdf/docx/xlsx）、文件大小，禁止 exe/sh 等可执行脚本；MinIO 访问权限隔离
XSS 过滤：前端输入框、后端存储统一转义特殊字符
用户密码加密存储，禁止明文；修改密码接口强制校验旧密码
日志脱敏：手机号、密钥、合同敏感信息脱敏输出
删除、批量操作、权限修改增加二次确认弹窗，同步写入操作审计日志
十、日志审计规范（永久留存，禁止清理）
三类日志永久存储：登录日志、操作审计日志、设备生命周期日志，禁止删除 / 清空表数据
触发日志场景：登录退出、设备增删改、状态变更、批量导入导出、附件上传删除、角色权限修改
日志必填字段：操作人、操作时间、操作 IP、操作类型、关联设备 ID、变更前后数据、备注
前端支持日志分页查询、Excel 导出，前端无删除日志权限