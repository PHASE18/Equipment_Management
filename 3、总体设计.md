# 第三章 总体设计

## 3.1 系统总体架构

### 3.1.1 建设目标

本系统采用 **B/S（Browser/Server）架构**，用户通过浏览器访问，实现设备档案、生命周期、项目迁移、维修、统计分析及 RBAC 权限控制。

以设备全生命周期管理为核心，整合基础信息、网络、配置、项目归属、维修、附件与审计日志，支撑设备管理数字化、规范化。

建设目标：

1. 统一设备档案库，信息电子化维护；
2. 覆盖采购→库存→上架→在用→停用/下架→报废全过程；
3. 维修与主状态并存（方案 A），故障可追踪、可统计；
4. 按部门 / 责任人实现数据范围隔离；
5. 首页统计与多维图表可视化；
6. 登录、操作、生命周期、迁移过程可审计追溯。

---

## 3.2 系统架构设计

采用前后端分离的三层架构。

```text
                ┌─────────────────────┐
                │     Web 浏览器       │
                │ Chrome / Edge 等     │
                └─────────┬───────────┘
                          │ HTTP/HTTPS
                ┌─────────▼───────────┐
                │  Vue3 前端           │
                │  Element Plus        │
                │  TypeScript / Pinia  │
                └─────────┬───────────┘
                          │ REST /api
                ┌─────────▼───────────┐
                │  Spring Boot 3       │
                │  JWT + RBAC          │
                │  MyBatis-Plus        │
                └─────────┬───────────┘
          ┌───────────────┴────────────────┐
 ┌────────▼────────┐              ┌────────▼────────┐
 │   MySQL 8        │              │ 附件存储         │
 │ equipment_manager│              │ local / MinIO    │
 └─────────────────┘              └─────────────────┘
```

技术栈摘要：

| 层次 | 技术 |
|------|------|
| 前端 | Vue 3、Vite、Element Plus、TypeScript、Pinia、ECharts、Playwright（E2E） |
| 后端 | Spring Boot 3、JWT、MyBatis-Plus、BCrypt |
| 数据 | MySQL 8（utf8mb4） |
| 附件 | 开发默认本地 `uploads/`；可配置 MinIO |

### 3.2.1 前端

主要职责：登录认证、动态菜单路由、表单校验、分页列表、生命周期/迁移弹窗、图表、文件上传下载。

### 3.2.2 后端

分层：`Controller → Service → Mapper → MySQL`。

横切能力：

- `JwtInterceptor`：Token 校验，加载用户权限；
- `PermissionInterceptor`：接口权限码校验（超管角色放行）；
- `DataPermissionHandler`：按数据范围自动改写 SQL。

### 3.2.3 数据库

库名 `equipment_manager`，第三范式设计。主要存储用户/部门/角色权限、设备档案与从属表、项目关联、维修、状态与迁移日志、操作/登录审计。脚本见 `sql/V1.0.0`～`V1.0.10`。

### 3.2.4 文件存储

支持设备照片、合同、文档、Excel、维修相关附件等。元数据存 `device_attachment`，文件体存本地或 MinIO（`application.yml` 中 `file.storage`）。

---

## 3.3 系统功能架构

```text
设备管理系统
├── 登录认证
├── 首页统计
├── 设备档案管理（含 IP / 配置 / Excel）
├── 生命周期管理
├── 设备迁移（换项目及字段变更明细）
├── 项目管理
├── 维修管理
├── 附件管理
├── 系统管理（用户 / 角色 / 部门 / 品牌 / 类型）
└── 日志审计
```

| 模块 | 功能描述 |
|------|----------|
| 登录认证 | 登录、退出、改密、JWT 续期；失败锁定 |
| 首页统计 | 设备总量、在用/维修中/停用/报废、故障与分布图 |
| 设备档案 | 增删改查、管理部门/使用部门、责任人、导入导出 |
| 生命周期 | 主状态合法流转 + 维修标志；状态历史 |
| 设备迁移 | 换项目并联动档案/网络/配置；仅记录有变更字段 |
| 项目管理 | 项目 CRUD、设备-项目绑定/同步 |
| 维修管理 | 工单、完成、故障类型统计；联动维修标志 |
| 附件管理 | 上传、列表、下载、删除（按设备/维修） |
| 系统管理 | 用户、角色、部门、品牌、设备类型、字典、配置 |
| 日志审计 | 登录日志、操作日志、生命周期日志 |

---

## 3.4 设备生命周期

主状态与「维修标志」分离：`status_code` + `maintaining_flag`。

```text
采购中 → 库存 → 待上架 → 在用
                              │
              ┌───────────────┼───────────────┐
              ▼               ▼               ▼
         (维修标志)           停用 ----------→下架
              │               │               │
              │◄──────────────┤               ├→ 报废
              ▼               ▼               │
             在用 ←───────────┘               └→ 在用（再上架）
```

规则：

| 规则 | 说明 |
|------|------|
| 主状态流转 | `IN_USE ↔ STOPPED`；`IN_USE/STOPPED → OFFLINE`；`OFFLINE → IN_USE/SCRAPPED` |
| 维修标志 | 仅在用、停用可开维修；展示「维修中/在用」「维修中/停用」 |
| 废弃状态 | `STANDBY` 迁为停用；`MAINTAINING` 不再作主状态 |
| 清标志 | 转入下架或报废时清除 `maintaining_flag` |
| 审计 | 主状态变更与进入/退出维修均写入 `device_status_log` |

---

## 3.5 设备迁移

设备更换所属项目时，可同时调整管理部门、使用部门、责任人/使用人、机房机柜、网络与配置等。

- 接口：`POST /api/device/migrate`
- 落库：`device_migration_log`（主表）+ `device_migration_item`（**仅有变更的字段**）
- 无变更提交拒绝；未改字段保持原值

---

## 3.6 权限与数据范围

### 3.6.1 RBAC

用户 ↔ 角色 ↔ 权限（菜单 / 按钮 / 接口）。前端菜单来自登录返回的 `menus`，按 `permissionCode` 动态注册路由。

### 3.6.2 预置角色

| 角色编码 | 名称 | 数据范围 |
|----------|------|----------|
| ADMIN | 系统管理员 | ALL |
| DEPT_ADMIN | 部门管理员 | DEPARTMENT |
| DEVICE_OWNER | 设备责任人 | SELF |

超管编码（`ADMIN` / `SUPER_ADMIN` 等）在权限校验时直接放行。

### 3.6.3 数据范围

| 范围 | 设备可见条件 |
|------|----------------|
| ALL | 不过滤 |
| DEPARTMENT | `device.department_id` = 用户部门 |
| SELF | `device.manager_user_id` = 用户 ID |

从属表（维修、附件、IP、配置、项目关联、状态/迁移日志）通过设备 ID 子查询继承同一范围。  
说明：`manager_name` / `use_user_name` 为展示文本，**不**参与 SELF 过滤。

---

## 3.7 安全设计要点

1. 密码 BCrypt 存储；登录失败次数与锁定时长可配置（`sys_config` / yml）；
2. 业务接口默认需 JWT；仅 `POST /api/login` 匿名；
3. 接口级权限码 + 数据范围双重约束；
4. 关键审计日志与登录日志，关键业务禁止随意物理删除审计数据。

---
