# 第三章 总体设计

## 3.1 系统总体架构

### 3.1.1 建设目标

本系统采用 **B/S（Browser/Server）架构**，用户无需安装客户端，仅通过浏览器即可访问系统，实现设备档案管理、生命周期管理、维修管理、统计分析及权限控制等功能。

系统以设备全生命周期管理为核心，结合设备基础信息、网络信息、配置信息、维修记录、附件资料及审计日志，形成统一的设备管理平台，为设备管理数字化、规范化提供支撑。

系统建设目标如下：

1. 建立统一设备档案库，实现设备信息电子化管理；
2. 实现设备采购、入库、上架、运行、维修、停用、报废全过程管理；
3. 建立设备维修档案，实现设备故障追踪与统计分析；
4. 实现不同部门的数据隔离和权限控制；
5. 实现设备信息查询、统计分析及数据可视化展示；
6. 支持Excel批量导入历史数据，降低系统上线成本；
7. 建立完整的操作审计日志，保证设备信息可追溯。

---

## 3.2 系统架构设计

本系统采用经典三层架构。

```text
                ┌─────────────────────┐
                │     Web浏览器        │
                │ Edge / Chrome       │
                └─────────┬───────────┘
                          │HTTP/HTTPS
                          │
                ┌─────────▼───────────┐
                │     Vue3 前端        │
                │ Element Plus 
                    TypeScript        │
                └─────────┬───────────┘
                          │REST API
                          │
                ┌─────────▼───────────┐
                │ Spring Boot 后端     │
                │ JWT认证             │
                │ MyBatis Plus        │
                └─────────┬───────────┘
                          │
          ┌───────────────┴────────────────┐
          │                                │
 ┌────────▼────────┐              ┌────────▼────────┐
 │     MySQL8      │              │      MinIO      │
 │   业务数据库     │              │  附件文件存储    │
 └─────────────────┘              └─────────────────┘
```

---

### 3.2.1 前端

采用 Vue3 + Element Plus 构建。

主要职责：

* 登录认证
* 页面展示
* 表单录入
* 数据校验
* 图表展示
* 文件上传
* Excel导入导出

特点：

* 响应速度快
* 界面统一
* 支持响应式布局
* 支持权限菜单动态生成

---

### 3.2.2 后端

采用 Spring Boot 构建REST API。

主要职责：

* 业务逻辑处理
* 权限控制
* 数据校验
* 数据统计
* Excel解析
* 文件管理
* 日志记录

采用分层架构：

```text
Controller

↓

Service

↓

Mapper

↓

MySQL
```

职责如下：

Controller

负责HTTP请求处理。

Service

负责业务逻辑。

Mapper

负责数据库访问。

---

### 3.2.3 数据库

采用 MySQL 8。

主要存储：

* 用户信息
* 部门信息
* 设备档案
* IP信息
* 配置信息
* 项目信息
* 维修记录
* 生命周期记录
* 操作日志
* 附件信息

数据库采用第三范式设计，减少数据冗余，提高扩展能力。

---

### 3.2.4 文件存储

系统支持上传：

* 设备照片
* 配置截图
* 验收报告
* 采购合同
* 维修报告
* 维保合同
* 机柜照片

附件统一存储于 MinIO（或本地文件服务器），数据库仅保存文件路径及元数据。

---

## 3.3 系统功能架构

系统划分为以下十一个功能模块：

```text
设备管理系统

├── 登录认证
│
├── 首页统计
│
├── 设备档案管理
│
├── 生命周期管理
│
├── IP地址管理
│
├── 项目管理
│
├── 维修管理
│
├── 附件管理
│
├── 统计分析
│
├── 系统管理
│
└── 日志审计
```

各模块职责如下：

| 模块     | 功能描述                    |
| ------ | ----------------------- |
| 登录认证   | 用户登录、退出、密码修改、JWT认证      |
| 首页统计   | 展示设备数量、状态统计、故障统计、图表分析   |
| 设备档案   | 设备新增、修改、删除、查询、导入导出      |
| 生命周期   | 管理设备状态流转及历史记录           |
| IP地址管理 | 管理业务IP、管理IP，自动校验唯一性     |
| 项目管理   | 管理项目及设备与项目关联关系          |
| 维修管理   | 记录维修全过程及故障分析            |
| 附件管理   | 管理设备相关文档和图片             |
| 统计分析   | 品牌、类型、部门、故障、维修费用等统计     |
| 系统管理   | 用户、角色、部门、品牌、设备类型等基础数据维护 |
| 日志审计   | 登录日志、操作日志、生命周期日志查询      |

---

## 3.4 系统业务流程

设备生命周期如下：

```text
采购中
   │
   ▼
库存
   │
   ▼
待上架
   │
   ▼
在用
 ┌─┴──────────────┐
 │                │
 ▼                ▼
维修中          备用
 │                │
 └──────┬─────────┘
        ▼
       在用
        │
        ▼
      停用
        │
        ▼
      报废
```

系统允许合法状态之间进行转换，每次状态变更均自动记录：

* 原状态；
* 新状态；
* 操作人；
* 操作时间；
* 变更原因；
* 备注信息。

形成完整的生命周期管理记录。

---

## 3.5 用户角色设计

系统采用基于角色的访问控制（RBAC）。

系统角色包括：

### （1）系统管理员

系统最高权限角色。

主要权限：

* 用户管理；
* 部门管理；
* 权限配置；
* 设备管理；
* 数据统计；
* Excel导入导出；
* 查看所有日志；
* 查看所有部门数据。

---

### （2）部门管理员

负责本部门设备管理。

主要权限：

* 查看本部门设备；
* 新增设备；
* 修改设备；
* 录入维修记录；
* 上传附件；
* 查看本部门统计信息；
* 导出本部门数据。

不得访问其他部门数据。

---

### （3）普通用户（设备责任人）

负责本人管理设备的信息维护。

主要权限：

* 查看本人负责设备；
* 修改本人负责设备部分信息；
* 新增维修记录；
* 上传维修附件；
* 查看设备历史记录。

不得删除设备，不得查看其他部门设备。

---

## 3.6 数据流设计

系统数据流如下：

```text
Excel导入
      │
      ▼
设备档案
      │
      ├────────► 配置信息
      │
      ├────────► IP地址
      │
      ├────────► 项目关联
      │
      ├────────► 附件资料
      │
      ├────────► 生命周期记录
      │
      └────────► 维修记录
                    │
                    ▼
               故障统计分析
                    │
                    ▼
               首页统计图表
```

所有业务数据均围绕设备档案展开，实现设备信息、维修记录、项目关联、附件资料及统计分析的统一管理。

---

## 3.7 系统部署设计

系统采用浏览器访问模式，部署方式如下：

| 组件     | 建议配置                                       |
| ------ | ------------------------------------------ |
| 操作系统   | Windows Server 2022 或 Linux（CentOS/Ubuntu） |
| JDK    | OpenJDK 21 LTS                             |
| Web服务器 | Spring Boot 内嵌 Tomcat                      |
| 数据库    | MySQL 8.0+                                 |
| 文件存储   | MinIO（或本地文件服务器）                            |
| 浏览器    | Microsoft Edge、Google Chrome（最新版）          |

建议采用前后端分离部署方式，数据库与文件存储独立部署，以提高系统稳定性和后续扩展能力。

---

## 3.8 系统设计原则

本系统遵循以下设计原则：

1. **统一管理原则**：所有设备档案、维修记录、附件及生命周期信息统一管理，避免信息分散。
2. **数据唯一原则**：设备编号、SN号、业务IP、管理IP等关键字段保持唯一，防止重复录入。
3. **权限隔离原则**：采用RBAC模型，实现部门级数据隔离和最小权限访问。
4. **全过程留痕原则**：所有新增、修改、删除及状态流转均记录审计日志，实现全程可追溯。
5. **易扩展原则**：采用模块化设计，支持后续增加扫码管理、告警提醒、自动发现等功能。
6. **高可维护性原则**：遵循统一编码规范和数据库设计规范，降低后期维护成本。
7. **安全可靠原则**：采用身份认证、权限控制、数据校验及日志审计等措施，保障系统安全稳定运行。
