可以，而且**建议直接生成 Apifox 的公共脚本 + 各接口 Pre-request Script + Tests Script**。

根据你上传的《设备管理系统接口设计说明书》，接口采用：

* RESTful
* JWT Token
* JSON
* 统一返回格式
* HTTP Code + Business Code
* 约67个接口

因此完全可以为 Apifox 做一套自动化测试。

---

# 推荐目录

```
Apifox

├── 公共环境变量
│      baseUrl
│      token
│      userId
│
├── 公共Pre-request
│      自动登录
│      自动刷新Token
│
├── 公共Tests
│      HTTP状态
│      code校验
│      message校验
│      响应时间
│
├── 登录模块
├── 用户模块
├── 部门模块
├── 设备模块
├── 生命周期
├── IP管理
├── 项目
├── 维修
├── 文件
├── Excel
├── 统计
├── 日志
├── 字典
└── 权限
```

---

# 一、公共Pre-request Script

放到整个项目即可。

```javascript
// 自动添加Token

let token = apifox.getVariable("token");

if (token) {
    apifox.setRequestHeader("Authorization", "Bearer " + token);
}

apifox.setRequestHeader("Content-Type", "application/json");
```

---

# 二、公共Tests Script（所有接口）

```javascript
// HTTP状态

pm.test("HTTP Status = 200", function () {
    pm.response.to.have.status(200);
});

// JSON

pm.test("Response is JSON", function () {
    pm.response.to.be.json;
});

// 响应时间

pm.test("Response Time < 1000ms", function () {
    pm.expect(pm.response.responseTime).to.be.below(1000);
});

let json = pm.response.json();

// code

pm.test("Business Code = 200", function () {
    pm.expect(json.code).to.eql(200);
});

// message

pm.test("Message = success", function () {
    pm.expect(json.message).to.eql("success");
});
```

对应统一返回规范。

---

# 三、登录接口 `/api/login`

根据接口设计：POST `/api/login`。

## Tests

```javascript
let json = pm.response.json();

pm.test("登录成功", function () {
    pm.expect(json.code).to.eql(200);
});

pm.test("返回Token", function () {
    pm.expect(json.data.token).to.not.eql(null);
});

// 保存Token

apifox.setVariable("token", json.data.token);

apifox.setVariable("userId", json.data.user.id);

apifox.setVariable("username", json.data.user.name);
```

---

# 四、获取当前用户

GET

```
/api/user/info
```

## Tests

```javascript
let json = pm.response.json();

pm.test("获取用户信息", function () {
    pm.expect(json.code).to.eql(200);
});

pm.test("用户ID存在", function () {
    pm.expect(json.data.id).to.not.eql(null);
});

pm.test("用户名存在", function () {
    pm.expect(json.data.name).to.not.eql("");
});
```

---

# 五、设备列表

GET

```
/api/device/list
```

接口设计包含分页返回 `total` 和 `records`。

## Tests

```javascript
let json = pm.response.json();

pm.test("设备列表成功", function () {
    pm.expect(json.code).to.eql(200);
});

pm.test("records存在", function () {
    pm.expect(json.data.records).to.be.an("array");
});

pm.test("total存在", function () {
    pm.expect(json.data.total).to.be.a("number");
});
```

---

# 六、新增设备

POST

```
/api/device
```

## Tests

```javascript
let json = pm.response.json();

pm.test("新增设备成功", function () {
    pm.expect(json.code).to.eql(200);
});

// 保存设备ID

apifox.setVariable("deviceId", json.data.id);
```

---

# 七、查询设备详情

GET

```
/api/device/{{deviceId}}
```

## Tests

```javascript
let json = pm.response.json();

pm.test("设备ID正确", function () {
    pm.expect(json.data.id).to.eql(Number(apifox.getVariable("deviceId")));
});

pm.test("设备编号存在", function () {
    pm.expect(json.data.deviceNo).to.not.eql("");
});
```

---

# 八、修改设备

PUT

```
/api/device
```

## Tests

```javascript
let json = pm.response.json();

pm.test("修改成功", function () {
    pm.expect(json.code).to.eql(200);
});
```

---

# 九、删除设备

DELETE

```
/api/device/{id}
```

## Tests

```javascript
let json = pm.response.json();

pm.test("删除成功", function () {
    pm.expect(json.code).to.eql(200);
});
```

---

# 十、IP管理

POST

```
/api/ip
```

接口要求自动校验业务 IP 和管理 IP 冲突。

## Tests

```javascript
let json = pm.response.json();

pm.test("IP保存成功", function () {
    pm.expect(json.code).to.eql(200);
});
```

### 重复IP测试

```javascript
pm.test("业务IP重复", function () {
    pm.expect(pm.response.json().code).to.eql(1007);
});
```

```javascript
pm.test("管理IP重复", function () {
    pm.expect(pm.response.json().code).to.eql(1008);
});
```

---

# 十一、生命周期

POST

```
/api/device/status/change
```

接口支持状态变更并记录生命周期日志。

## Tests

```javascript
let json = pm.response.json();

pm.test("状态修改成功", function () {
    pm.expect(json.code).to.eql(200);
});
```

非法流转：

```javascript
pm.test("非法生命周期", function () {
    pm.expect(pm.response.json().code).to.eql(1014);
});
```

---

# 十二、维修模块

POST

```
/api/maintenance
```

## Tests

```javascript
let json = pm.response.json();

pm.test("维修记录新增成功", function () {
    pm.expect(json.code).to.eql(200);
});

apifox.setVariable("maintenanceId", json.data.id);
```

---

# 十三、Excel导入

POST

```
/api/excel/import
```

## Tests

```javascript
let json = pm.response.json();

pm.test("导入成功", function () {
    pm.expect(json.code).to.eql(200);
});
```

错误文件：

```javascript
pm.test("Excel格式错误", function () {
    pm.expect(pm.response.json().code).to.eql(1012);
});
```

---

# 十四、统计接口

例如：

```
GET /api/statistics/home
```

## Tests

```javascript
let json = pm.response.json();

pm.test("首页统计成功", function () {
    pm.expect(json.code).to.eql(200);
});

pm.test("设备总数存在", function () {
    pm.expect(json.data.deviceTotal).to.be.a("number");
});
```

---

# 十五、附件上传

POST

```
/api/file/upload
```

## Tests

```javascript
let json = pm.response.json();

pm.test("上传成功", function () {
    pm.expect(json.code).to.eql(200);
});

pm.test("返回fileId", function () {
    pm.expect(json.data.fileId).to.not.eql(null);
});
```

---

## 建议增加自动化测试场景

除了单接口测试，还可以在 Apifox 中配置完整的业务流程：

| 场景     | 执行顺序                                      |
| ------ | ----------------------------------------- |
| 登录流程   | 登录 → 获取 Token → 查询当前用户                    |
| 设备生命周期 | 新增设备 → 查询详情 → 修改设备 → 修改状态 → 查询生命周期 → 删除设备 |
| IP 管理  | 新增设备 → 配置 IP → 验证重复 IP → 删除设备             |
| 项目管理   | 新增项目 → 绑定设备 → 查询项目 → 解绑设备 → 删除项目          |
| 维修流程   | 新增设备 → 新增维修记录 → 查询维修 → 修改维修 → 删除维修        |
| Excel  | 下载模板 → 导入 → 查询设备 → 导出                     |
| 权限验证   | 普通用户访问管理员接口（403）→ 管理员访问成功                 |
| 日志审计   | 执行新增/修改/删除操作 → 查询操作日志 → 验证日志生成            |

这套接口文档约包含 **67 个 RESTful API**，可以进一步生成一套**可直接导入 Apifox 的完整测试脚本（约 3000～5000 行 JavaScript）**，覆盖每个接口的成功、失败、权限、参数校验和业务异常测试。
