# 设备管理系统数据库设计说明书

**Database Design Specification**

| 项 | 内容 |
|----|------|
| 版本 | V1.0.10（与 `sql/` 脚本终态一致） |
| 数据库 | MySQL 8.0+ |
| 库名 | `equipment_manager` |
| 字符集 / 排序规则 | `utf8mb4` / `utf8mb4_unicode_ci` |
| 存储引擎 | InnoDB |
| 业务表数量 | 20 |

---

## 1. 设计目标与原则

支撑设备全生命周期管理：档案、配置、网络、项目绑定、维修、附件、状态流转、项目迁移、RBAC 与审计。

原则：

- 第三范式；字典编码解耦枚举
- 主外键完整性（脚本声明 FK，可按环境裁剪）
- 关键字段唯一约束（用户名、设备编号、SN、项目编码、IP 等）
- 业务主表普遍逻辑删除（`deleted`：0/1）
- 审计字段：`create_by` / `create_time` / `update_by` / `update_time`
- 生命周期方案 A：`status_code`（主状态）+ `maintaining_flag`（维修标志）

---

## 2. 模块与表清单

### 2.1 系统管理（8）

| 表名 | 说明 |
|------|------|
| `sys_department` | 部门（树） |
| `sys_user` | 用户 |
| `sys_role` | 角色 |
| `sys_permission` | 权限（菜单/按钮/接口） |
| `sys_user_role` | 用户-角色 |
| `sys_role_permission` | 角色-权限 |
| `sys_dict` | 数据字典 |
| `sys_config` | 系统配置 |

### 2.2 设备管理（9）

| 表名 | 说明 |
|------|------|
| `device` | 设备主表 |
| `device_config` | 设备配置（1:1） |
| `device_ip` | 设备网络（1:1） |
| `project` | 项目 |
| `device_project` | 设备-项目 |
| `device_attachment` | 附件元数据 |
| `device_status_log` | 生命周期变更日志 |
| `device_migration_log` | 设备迁移主表 |
| `device_migration_item` | 迁移字段明细 |

### 2.3 维修（1）

| 表名 | 说明 |
|------|------|
| `device_maintenance` | 维修工单 |

### 2.4 日志（2）

| 表名 | 说明 |
|------|------|
| `sys_operation_log` | 操作审计 |
| `sys_login_log` | 登录日志 |

---

## 3. 关系概览（ER）

```text
sys_department ──< sys_user ──< sys_user_role >── sys_role ──< sys_role_permission >── sys_permission
       │              │
       │              │ manager_user_id（SELF）
       │              ▼
       └────────── device ◄── department_id（DEPARTMENT / 管理部门）
                     │
     ┌───────┬───────┼───────┬────────────┬──────────────┬─────────────────┐
     ▼       ▼       ▼       ▼            ▼              ▼                 ▼
  config    ip   device_project  maintenance  attachment  status_log   migration_log
                     │                                         │                 │
                     ▼                                         │                 ▼
                  project                                      │          migration_item
                                                               │
sys_dict ← brand / type / status / fault_type / attachment_type
sys_config ← 登录锁定等
sys_operation_log / sys_login_log ← 审计
```

---

## 4. 数据权限相关字段

| 数据范围 | 典型角色 | 设备过滤 |
|----------|----------|----------|
| ALL | ADMIN | 不过滤 |
| DEPARTMENT | DEPT_ADMIN | `device.department_id` = 用户部门 |
| SELF | DEVICE_OWNER | `device.manager_user_id` = 用户 ID |

说明：

- `manager_user_id`：SELF 依据；`manager_name` 仅展示
- `use_user_name` / `use_department_id`：不参与当前数据范围
- 维修、附件、IP、配置、项目关联、状态/迁移日志：经 `device_id` 继承设备范围

---

## 5. 字典约定（`sys_dict`）

| dict_type | 用途 | 关联字段 |
|-----------|------|----------|
| `device_brand` | 品牌 | `device.brand_code` |
| `device_type` | 设备类型 | `device.device_type_code` |
| `device_status` | 主状态 | `device.status_code`、状态日志 |
| `fault_type` | 故障类型 | `device_maintenance.fault_type_code` |
| `attachment_type` | 附件类型 | `device_attachment.file_type_code` |

### 5.1 `device_status` 主状态编码（V1.0.10）

| dict_code | 名称 | 启用 |
|-----------|------|------|
| PURCHASING | 采购中 | 是 |
| IN_STOCK | 库存 | 是 |
| PENDING_ONLINE | 待上架 | 是 |
| IN_USE | 在用 | 是 |
| STOPPED | 停用(机柜) | 是 |
| OFFLINE | 下架(库房) | 是 |
| SCRAPPED | 报废 | 是 |
| MAINTAINING | 维修中(历史) | 否（仅兼容） |
| STANDBY | 备用(历史) | 否（仅兼容） |

维修展示态由 `maintaining_flag=1` 与主状态组合，不单独占主状态码。

`fault_type` 初始值见 `V1.0.5`：HARDWARE / SOFTWARE / NETWORK / POWER / OTHER。

---

## 6. 分表设计（终态）

下列「空」列：N=非空，Y=可空。AI=自增。

### 6.1～6.8 系统表摘要

| 表 | 要点 |
|----|------|
| `sys_department` | `parent_id` 树；审计 + deleted |
| `sys_user` | `uk_username`；密码 BCrypt；`department_id`；status |
| `sys_role` | `uk_role_code`（ADMIN / DEPT_ADMIN / DEVICE_OWNER） |
| `sys_permission` | `uk_permission_code`；type：1 菜单 / 2 按钮 / 3 接口 |
| `sys_user_role` | PK `(user_id, role_id)` |
| `sys_role_permission` | PK `(role_id, permission_id)` |
| `sys_dict` | UK `(dict_type, dict_code)` |
| `sys_config` | UK `config_key`（如登录失败次数） |

字段级明细与 V1.0.0 一致，此处不重复罗列。

---

### 6.9 `device`（设备主表，终态）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| device_no | VARCHAR(50) | N | | 设备编号，UK |
| device_name | VARCHAR(100) | N | | 设备名称 |
| sn | VARCHAR(100) | Y | | 序列号，UK |
| asset_no | VARCHAR(100) | Y | | 资产编号 |
| is_fixed_asset | TINYINT | N | 0 | 是否固定资产 |
| brand_code | VARCHAR(50) | Y | | → device_brand |
| model | VARCHAR(100) | Y | | 型号 |
| device_type_code | VARCHAR(50) | Y | | → device_type |
| department_id | BIGINT | Y | | **管理部门**（DEPARTMENT 范围） |
| use_department_id | BIGINT | Y | | 使用部门 |
| manager_user_id | BIGINT | Y | | **责任人用户 ID**（SELF 范围） |
| manager_name | VARCHAR(100) | Y | | 责任人文本 |
| use_user_name | VARCHAR(100) | Y | | 使用人文本 |
| original_value | DECIMAL(14,2) | Y | | 原值 |
| approval_no | VARCHAR(100) | Y | | 批准文号 |
| supplier | VARCHAR(100) | Y | | 供应商 |
| maintenance_company | VARCHAR(100) | Y | | 维保单位 |
| purchase_date | DATE | Y | | 采购日期 |
| manufacture_date | DATE | Y | | 出厂日期 |
| online_date | DATE | Y | | 上架日期 |
| warranty_end | DATE | Y | | 到保日期 |
| scrap_date | DATE | Y | | 报废日期 |
| status_code | VARCHAR(50) | N | | **主状态** → device_status |
| maintaining_flag | TINYINT | N | 0 | **维修标志** 0/1 |
| cabinet | VARCHAR(100) | Y | | 机柜 U 位 |
| location | VARCHAR(200) | Y | | 所在机房 |
| remark | VARCHAR(500) | Y | | 备注 |
| 审计字段 + deleted | | | | |

索引：`uk_device_no`，`uk_sn`，`idx_status_code`，`idx_department_id`，`idx_manager_user_id`，`idx_device_type_code`，`idx_brand_code`，`idx_purchase_date`，`idx_warranty_end`，`idx_use_department_id`  
外键：`department_id` → 部门；`manager_user_id` → 用户（ON DELETE SET NULL）

---

### 6.10 `device_config`（1:1）

| 字段 | 说明 |
|------|------|
| device_id | UK，FK → device CASCADE |
| cpu / memory / disk / raid | 硬件 |
| gpu / fiber_card / nic / power_supply | 扩展硬件 |
| os / db_version / firmware / bios | 软件/固件 |
| remark | 备注 |
| 审计 + deleted | |

---

### 6.11 `device_ip`（1:1）

| 字段 | 说明 |
|------|------|
| device_id | UK，FK CASCADE |
| business_ip / management_ip | UK（可空多 NULL） |
| mask / gateway | |
| mounted_business / network_zone / mgmt_login_method | 业务网络信息 |
| 审计 + deleted | |

---

### 6.12 `project`

`project_name`、`project_code`（UK）、`department_id`、remark、审计 + deleted。

### 6.13 `device_project`

PK `(device_id, project_id)`，`create_time`。FK → device / project CASCADE。

---

### 6.14 `device_maintenance`

| 字段 | 说明 |
|------|------|
| device_id | FK CASCADE |
| maintenance_date | 必填 |
| maintenance_person / maintenance_company | |
| fault_type_code | → fault_type |
| fault_reason / fault_description / replace_parts | |
| maintenance_cost | DECIMAL |
| recover_date / is_resolved | 完成态 |
| attachment_path | 历史路径字段 |
| remark、审计 + deleted | |

开单：仅主状态为 IN_USE/STOPPED 时允许，并置设备 `maintaining_flag=1`。  
结单：无其它未结工单时清零标志。

---

### 6.15 `device_attachment`

| 字段 | 说明 |
|------|------|
| device_id | 必填 |
| maintenance_id | 可空，关联工单 |
| file_name / file_type_code / file_size / file_path | |
| upload_user_id / upload_time | |
| 审计 + deleted | |

---

### 6.16 `device_status_log`

| 字段 | 说明 |
|------|------|
| device_id | 设备 |
| old_status_code / new_status_code | 主状态（进退维修时可为同码，靠 reason/remark 区分） |
| change_reason / remark | |
| operator_id / change_time | |

无 `deleted`（追加式日志）。

---

### 6.17 `device_migration_log`（V1.0.9）

| 字段 | 类型 | 空 | 说明 |
|------|------|----|------|
| id | BIGINT | N | AI |
| device_id | BIGINT | N | 设备 |
| from_project_ids | VARCHAR(500) | Y | 迁出前项目 ID（逗号分隔） |
| to_project_ids | VARCHAR(500) | Y | 迁入后项目 ID |
| reason | VARCHAR(500) | N | 迁移原因 |
| remark | VARCHAR(500) | Y | 备注 |
| operator_id | BIGINT | Y | 操作人 |
| migrate_time | DATETIME | N | 迁移时间 |

### 6.18 `device_migration_item`（V1.0.9）

| 字段 | 类型 | 空 | 说明 |
|------|------|----|------|
| id | BIGINT | N | AI |
| migration_id | BIGINT | N | → migration_log |
| field_group | VARCHAR(50) | N | BASIC / NETWORK / CONFIG / PROJECT |
| field_key | VARCHAR(100) | N | 字段键 |
| field_label | VARCHAR(100) | N | 中文名 |
| old_value / new_value | VARCHAR(1000) | Y | 变更前后 |

仅写入**有变更**的字段行。

---

### 6.19 `sys_operation_log` / 6.20 `sys_login_log`

操作日志：`operator_id`、`operation_type`、`table_name`、`business_id`、前后 JSON、ip、browser、create_time。  
登录日志：`username`、`login_ip`、`browser`、`login_time`、`result`（0 失败 / 1 成功）。

---

## 7. 主外键一览（含增量）

| 约束 | 从表.列 | 主表 | 删除 |
|------|---------|------|------|
| fk_user_department | sys_user.department_id | sys_department | SET NULL |
| fk_user_role_* | sys_user_role | user / role | CASCADE |
| fk_role_perm_* | sys_role_permission | role / permission | CASCADE |
| fk_device_department | device.department_id | sys_department | SET NULL |
| fk_device_manager | device.manager_user_id | sys_user | SET NULL |
| fk_device_config_device | device_config.device_id | device | CASCADE |
| fk_device_ip_device | device_ip.device_id | device | CASCADE |
| fk_project_department | project.department_id | sys_department | SET NULL |
| fk_device_project_* | device_project | device / project | CASCADE |
| fk_maintenance_device | device_maintenance.device_id | device | CASCADE |
| fk_attachment_device | device_attachment.device_id | device | CASCADE |
| fk_attachment_upload_user | upload_user_id | sys_user | SET NULL |
| fk_status_log_* | device_status_log | device / sys_user | CASCADE / SET NULL |
| fk_migration_log_* | device_migration_log | device / sys_user | CASCADE / SET NULL |
| fk_migration_item_log | device_migration_item.migration_id | migration_log | CASCADE |
| fk_operation_log_operator | sys_operation_log.operator_id | sys_user | SET NULL |

未建物理 FK：`device.use_department_id`、`device_attachment.maintenance_id`（应用层保证）。

---

## 8. 命名规范

| 类别 | 约定 |
|------|------|
| 表名 | 小写蛇形；系统 `sys_`；设备域 `device_` |
| 字段 | 小写蛇形；主键 `id`；外键 `*_id`；字典 `*_code` |
| 索引 | `uk_*` / `idx_*` / `fk_*` |

---

## 9. 脚本版本与部署顺序

```text
sql/
├── V1.0.0__init_schema.sql              # 建库 + 核心表 + 外键
├── V1.0.1__init_auth_data.sql           # 部门、ADMIN、菜单、登录配置
├── V1.0.2__init_rbac_data.sql           # DEPT_ADMIN / DEVICE_OWNER、按钮权限、测试账号
├── V1.0.3__init_system_menu.sql         # 系统管理子菜单、示例品牌/类型
├── V1.0.4__device_status_log_remark.sql # status_log.remark
├── V1.0.5__maintenance_enhance.sql      # attachment.maintenance_id + fault_type
├── V1.0.6__device_fields_enhance.sql    # device / config / ip 字段增强
├── V1.0.7__remove_statistics_menu.sql   # 去掉重复统计菜单
├── V1.0.8__device_manager_name.sql      # device.manager_name
├── V1.0.9__device_migration_log.sql     # 迁移主表 + 明细表
└── V1.0.10__device_lifecycle_scheme_a.sql # maintaining_flag；状态迁移；device_status 字典
```

执行注意：

1. 按版本号顺序执行；客户端 `--default-character-set=utf8mb4` 或 `SET NAMES utf8mb4`
2. `CREATE TABLE IF NOT EXISTS` **不会**升级已有表结构；旧库必须跑增量 `ALTER`
3. V1.0.10 幂等增加 `maintaining_flag`；历史 `MAINTAINING`→`IN_USE`+标志；`STANDBY`→`STOPPED`
4. 密码字段建议 `VARCHAR(255)` 以容纳 BCrypt

---
