# 设备管理系统数据库设计说明书

**Database Design Specification**

| 项 | 内容 |
|----|------|
| 版本 | V1.0.8（与 `sql/` 脚本终态一致） |
| 数据库 | MySQL 8.0+ |
| 库名 | `equipment_manager` |
| 字符集 / 排序规则 | `utf8mb4` / `utf8mb4_unicode_ci` |
| 存储引擎 | InnoDB |
| 业务表数量 | 18 |

---

## 1. 设计目标与原则

支撑设备全生命周期管理：档案、配置、网络、项目绑定、维修、附件、状态流转、RBAC 权限与审计日志。

设计原则：

- 第三范式（3NF），字典编码解耦枚举值
- 主外键完整性（脚本末尾声明 FK，可按环境裁剪）
- 业务关键字段唯一约束（用户名、设备编号、SN、项目编码等）
- 业务主表普遍支持逻辑删除（`deleted`：0 未删 / 1 已删）
- 审计字段：`create_by` / `create_time` / `update_by` / `update_time`

---

## 2. 模块与表清单

### 2.1 系统管理（8）

| 表名 | 说明 |
|------|------|
| `sys_department` | 部门（树） |
| `sys_user` | 用户 |
| `sys_role` | 角色 |
| `sys_permission` | 权限（菜单/按钮/接口） |
| `sys_user_role` | 用户-角色 |
| `sys_role_permission` | 角色-权限 |
| `sys_dict` | 数据字典 |
| `sys_config` | 系统配置 |

### 2.2 设备管理（7）

| 表名 | 说明 |
|------|------|
| `device` | 设备主表 |
| `device_config` | 设备配置（1:1） |
| `device_ip` | 设备 IP / 网络（1:1） |
| `project` | 项目 |
| `device_project` | 设备-项目 |
| `device_attachment` | 附件 |
| `device_status_log` | 生命周期状态变更日志 |

### 2.3 维修（1）

| 表名 | 说明 |
|------|------|
| `device_maintenance` | 维修工单 |

### 2.4 日志（2）

| 表名 | 说明 |
|------|------|
| `sys_operation_log` | 操作审计 |
| `sys_login_log` | 登录日志 |

---

## 3. 关系概览（ER）

```text
sys_department ──< sys_user ──< sys_user_role >── sys_role ──< sys_role_permission >── sys_permission
       │              │
       │              │ manager_user_id（SELF 数据范围）
       │              ▼
       └────────── device ◄── department_id（DEPARTMENT 数据范围 / 管理部门）
                     │
        ┌────────────┼────────────┬──────────────┬────────────────┐
        ▼            ▼            ▼              ▼                ▼
  device_config  device_ip  device_project  device_maintenance  device_attachment
                     │            │              │                    │
                     │            ▼              │                    │
                     │         project           │◄── maintenance_id ─┘
                     │                           │
                     └──── device_status_log ◄───┘（按 device_id）

sys_dict   ← brand_code / device_type_code / status_code / fault_type_code / file_type_code
sys_config ← 登录锁定等运行时配置
sys_operation_log / sys_login_log ← 审计
```

---

## 4. 数据权限相关字段（重要）

后端数据范围由角色编码解析，过滤字段如下（**与表单展示字段区分**）：

| 数据范围 | 典型角色编码 | 设备过滤条件 |
|----------|--------------|--------------|
| ALL | `ADMIN` / `SUPER_ADMIN` 等 | 不过滤 |
| DEPARTMENT | `DEPT_ADMIN` | `device.department_id` = 当前用户部门 |
| SELF | `DEVICE_OWNER` | `device.manager_user_id` = 当前用户 ID |

说明：

- `manager_user_id`：责任人**用户 ID**，SELF 权限依据
- `manager_name`：责任人**自由文本**（展示），不参与过滤
- `use_user_name`：使用人文本，不参与过滤
- `use_department_id`：使用部门，不参与当前数据范围过滤

---

## 5. 字典约定（`sys_dict`）

业务编码字段通过 `(dict_type, dict_code)` 关联字典，不建物理 FK。

| dict_type | 用途 | 关联字段示例 |
|-----------|------|----------------|
| `device_brand` | 品牌 | `device.brand_code` |
| `device_type` | 设备类型 | `device.device_type_code` |
| `device_status` | 设备状态 | `device.status_code`、状态日志 |
| `fault_type` | 故障类型 | `device_maintenance.fault_type_code` |
| `attachment_type` | 附件类型 | `device_attachment.file_type_code` |

`fault_type` 初始值见 `V1.0.5`（HARDWARE / SOFTWARE / NETWORK / POWER / OTHER）。

---

## 6. 分表设计（终态）

下列「空」列：N=非空，Y=可空。AI=自增。

### 6.1 `sys_department`（部门表）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| department_name | VARCHAR(500) | N | | 部门名称 |
| parent_id | BIGINT | N | 0 | 父部门，0=顶级 |
| leader | VARCHAR(50) | Y | | 负责人 |
| phone | VARCHAR(30) | Y | | 电话 |
| remark | VARCHAR(200) | Y | | 备注 |
| create_by / create_time / update_by / update_time | | | | 审计 |
| deleted | TINYINT | N | 0 | 逻辑删除 |

索引：`PRIMARY(id)`，`idx_parent_id(parent_id)`

---

### 6.2 `sys_user`（用户表）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| username | VARCHAR(100) | N | | 登录名，UK |
| password | VARCHAR(255) | N | | BCrypt |
| real_name | VARCHAR(300) | Y | | 真实姓名（支持中文，库需 utf8mb4） |
| department_id | BIGINT | Y | | 所属部门 |
| phone | VARCHAR(30) | Y | | 手机 |
| email | VARCHAR(100) | Y | | 邮箱 |
| status | TINYINT | N | 1 | 0 禁用 / 1 启用 |
| create_by / create_time / update_by / update_time | | | | 审计 |
| deleted | TINYINT | N | 0 | 逻辑删除 |

索引：`PRIMARY`，`uk_username`，`idx_department_id`，`idx_status`  
外键：`department_id` → `sys_department.id`（ON DELETE SET NULL）

---

### 6.3 `sys_role`（角色表）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| role_name | VARCHAR(300) | N | | 角色名称 |
| role_code | VARCHAR(50) | N | | 角色编码，UK；影响数据范围与超管判定 |
| remark | VARCHAR(200) | Y | | 备注 |
| 审计字段 + deleted | | | | |

种子编码：`ADMIN`、`DEPT_ADMIN`、`DEVICE_OWNER`（见 `V1.0.1` / `V1.0.2`）。

---

### 6.4 `sys_permission`（权限表）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| parent_id | BIGINT | N | 0 | 父权限 |
| permission_name | VARCHAR(100) | N | | 名称 |
| permission_code | VARCHAR(100) | N | | 编码，UK（如 `device:list`） |
| permission_type | TINYINT | N | | 1 菜单 / 2 按钮 / 3 接口 |
| path | VARCHAR(200) | Y | | 前端路由或接口路径 |
| icon | VARCHAR(100) | Y | | 菜单图标 |
| sort | INT | N | 0 | 排序 |
| status | TINYINT | N | 1 | 0 禁用 / 1 启用 |
| 审计字段 + deleted | | | | |

索引：`PRIMARY`，`uk_permission_code`，`idx_parent_id`，`idx_permission_type`

---

### 6.5 `sys_user_role`（用户角色）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| user_id | BIGINT | N | | 用户 ID |
| role_id | BIGINT | N | | 角色 ID |
| create_time | DATETIME | N | CURRENT_TIMESTAMP | 创建时间 |

主键：`(user_id, role_id)`；索引：`idx_role_id`  
外键：级联删除用户/角色时清理关联

---

### 6.6 `sys_role_permission`（角色权限）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| role_id | BIGINT | N | | 角色 ID |
| permission_id | BIGINT | N | | 权限 ID |
| create_time | DATETIME | N | CURRENT_TIMESTAMP | |

主键：`(role_id, permission_id)`；索引：`idx_permission_id`

---

### 6.7 `sys_dict`（字典表）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| dict_type | VARCHAR(50) | N | | 类型 |
| dict_code | VARCHAR(50) | N | | 编码 |
| dict_name | VARCHAR(100) | N | | 名称 |
| sort | INT | N | 0 | 排序 |
| status | TINYINT | N | 1 | 启用状态 |
| 审计字段 + deleted | | | | |

唯一：`uk_dict_type_code(dict_type, dict_code)`

---

### 6.8 `sys_config`（系统配置）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| config_key | VARCHAR(100) | N | | 配置键，UK |
| config_value | VARCHAR(500) | N | | 配置值 |
| remark | VARCHAR(200) | Y | | 备注 |
| 审计字段 + deleted | | | | |

示例：`auth.login.max-failures`、`auth.login.lock-duration-minutes`

---

### 6.9 `device`（设备主表，终态）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| device_no | VARCHAR(50) | N | | 设备编号，UK |
| device_name | VARCHAR(100) | N | | 设备名称 |
| sn | VARCHAR(100) | Y | | 序列号，UK（允许多 NULL，视 MySQL 唯一索引行为） |
| asset_no | VARCHAR(100) | Y | | 资产编号 |
| is_fixed_asset | TINYINT | N | 0 | 是否固定资产 |
| brand_code | VARCHAR(50) | Y | | 品牌编码 → dict `device_brand` |
| model | VARCHAR(100) | Y | | 型号 |
| device_type_code | VARCHAR(50) | Y | | 类型编码 → dict `device_type` |
| department_id | BIGINT | Y | | **管理部门** ID（DEPARTMENT 范围） |
| use_department_id | BIGINT | Y | | 使用部门 ID |
| manager_user_id | BIGINT | Y | | **责任人用户 ID**（SELF 范围） |
| manager_name | VARCHAR(100) | Y | | 责任人（文本展示） |
| use_user_name | VARCHAR(100) | Y | | 使用人（文本，不做权限） |
| original_value | DECIMAL(14,2) | Y | | 设备原值 |
| approval_no | VARCHAR(100) | Y | | 批准文号 |
| supplier | VARCHAR(100) | Y | | 供应商 |
| maintenance_company | VARCHAR(100) | Y | | 维保单位 |
| purchase_date | DATE | Y | | 采购日期 |
| manufacture_date | DATE | Y | | 出厂日期 |
| online_date | DATE | Y | | 上架日期 |
| warranty_end | DATE | Y | | 到保日期 |
| scrap_date | DATE | Y | | 报废日期 |
| status_code | VARCHAR(50) | N | | 主状态编码 → dict `device_status` |
| maintaining_flag | TINYINT | N | 0 | 是否维修中：0否1是（可与在用/停用并存） |
| cabinet | VARCHAR(100) | Y | | 机柜 U 位 |
| location | VARCHAR(200) | Y | | 所在机房 |
| remark | VARCHAR(500) | Y | | 备注 |
| 审计字段 + deleted | | | | |

索引：`uk_device_no`，`uk_sn`，`idx_status_code`，`idx_department_id`，`idx_manager_user_id`，`idx_device_type_code`，`idx_brand_code`，`idx_purchase_date`，`idx_warranty_end`，`idx_use_department_id`  
外键：`department_id` → 部门；`manager_user_id` → 用户（ON DELETE SET NULL）

---

### 6.10 `device_config`（设备配置，1:1）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| device_id | BIGINT | N | | 设备 ID，UK |
| cpu / memory / disk / raid | VARCHAR | Y | | 硬件 |
| gpu / fiber_card / nic / power_supply | VARCHAR(200) | Y | | 扩展硬件 |
| os / db_version / firmware / bios | VARCHAR | Y | | 软件/固件 |
| remark | VARCHAR(500) | Y | | 备注 |
| 审计字段 + deleted | | | | |

外键：`device_id` → `device.id`（ON DELETE CASCADE）

---

### 6.11 `device_ip`（设备网络，1:1）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| device_id | BIGINT | N | | 设备 ID，UK |
| business_ip | VARCHAR(45) | Y | | 业务 IP，UK |
| management_ip | VARCHAR(45) | Y | | 管理 IP，UK |
| mask / gateway | VARCHAR(45) | Y | | 掩码 / 网关 |
| mounted_business | VARCHAR(200) | Y | | 挂载业务 |
| network_zone | VARCHAR(100) | Y | | 所属网络 |
| mgmt_login_method | VARCHAR(100) | Y | | 管理地址登录方式 |
| 审计字段 + deleted | | | | |

外键：`device_id` → `device.id`（CASCADE）

---

### 6.12 `project`（项目）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| project_name | VARCHAR(100) | N | | 项目名称 |
| project_code | VARCHAR(50) | N | | 项目编码，UK |
| department_id | BIGINT | Y | | 所属部门 |
| remark | VARCHAR(500) | Y | | 备注 |
| 审计字段 + deleted | | | | |

---

### 6.13 `device_project`（设备-项目）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| device_id | BIGINT | N | | 设备 |
| project_id | BIGINT | N | | 项目 |
| create_time | DATETIME | N | CURRENT_TIMESTAMP | |

主键：`(device_id, project_id)`

---

### 6.14 `device_maintenance`（维修）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| device_id | BIGINT | N | | 设备 |
| maintenance_date | DATE | N | | 维修日期 |
| maintenance_person | VARCHAR(50) | Y | | 维修人员 |
| maintenance_company | VARCHAR(100) | Y | | 维修单位 |
| fault_type_code | VARCHAR(50) | Y | | 故障类型 → dict `fault_type` |
| fault_reason | VARCHAR(200) | Y | | 故障原因 |
| fault_description | TEXT | Y | | 故障描述 |
| replace_parts | VARCHAR(500) | Y | | 更换配件 |
| maintenance_cost | DECIMAL(12,2) | Y | | 费用 |
| recover_date | DATE | Y | | 恢复日期 |
| is_resolved | TINYINT | N | 0 | 是否已解决 |
| attachment_path | VARCHAR(500) | Y | | 历史附件路径字段 |
| remark | VARCHAR(500) | Y | | 备注 |
| 审计字段 + deleted | | | | |

索引：`idx_device_id`，`idx_fault_type_code`，`idx_maintenance_date`

---

### 6.15 `device_attachment`（附件，终态）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| device_id | BIGINT | N | | 设备 |
| maintenance_id | BIGINT | Y | | 关联维修工单（可空） |
| file_name | VARCHAR(200) | N | | 文件名 |
| file_type_code | VARCHAR(50) | N | | 类型编码 → dict / 业务编码 |
| file_size | BIGINT | Y | | 字节数 |
| file_path | VARCHAR(500) | N | | 存储路径 |
| upload_user_id | BIGINT | Y | | 上传人 |
| upload_time | DATETIME | N | CURRENT_TIMESTAMP | 上传时间 |
| 审计字段 + deleted | | | | |

索引：`idx_device_id`，`idx_maintenance_id`，`idx_file_type_code`，`idx_upload_user_id`

---

### 6.16 `device_status_log`（生命周期日志，终态）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| device_id | BIGINT | N | | 设备 |
| old_status_code | VARCHAR(50) | Y | | 原状态 |
| new_status_code | VARCHAR(50) | N | | 新状态 |
| change_reason | VARCHAR(500) | Y | | 变更原因 |
| remark | VARCHAR(500) | Y | | 备注 |
| operator_id | BIGINT | Y | | 操作人 |
| change_time | DATETIME | N | CURRENT_TIMESTAMP | 变更时间 |

无逻辑删除字段（追加式日志）。

---

### 6.17 `sys_operation_log`（操作日志）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| operator_id | BIGINT | Y | | 操作人 |
| operation_type | VARCHAR(50) | N | | INSERT/UPDATE/DELETE 等 |
| table_name | VARCHAR(100) | N | | 业务表名 |
| business_id | BIGINT | Y | | 业务主键 |
| before_json / after_json | JSON | Y | | 变更快照 |
| ip | VARCHAR(45) | Y | | 客户端 IP |
| browser | VARCHAR(200) | Y | | UA |
| create_time | DATETIME | N | CURRENT_TIMESTAMP | |

---

### 6.18 `sys_login_log`（登录日志）

| 字段 | 类型 | 空 | 默认 | 说明 |
|------|------|----|------|------|
| id | BIGINT | N | AI | 主键 |
| username | VARCHAR(50) | N | | 登录名 |
| login_ip | VARCHAR(45) | Y | | IP |
| browser | VARCHAR(200) | Y | | UA |
| login_time | DATETIME | N | CURRENT_TIMESTAMP | |
| result | TINYINT | N | 1 | 0 失败 / 1 成功 |

---

## 7. 主外键一览

| 约束名 | 从表.列 | 主表.列 | 删除策略 |
|--------|---------|---------|----------|
| fk_user_department | sys_user.department_id | sys_department.id | SET NULL |
| fk_user_role_user / _role | sys_user_role.* | sys_user / sys_role | CASCADE |
| fk_role_perm_role / _permission | sys_role_permission.* | sys_role / sys_permission | CASCADE |
| fk_device_department | device.department_id | sys_department.id | SET NULL |
| fk_device_manager | device.manager_user_id | sys_user.id | SET NULL |
| fk_device_config_device | device_config.device_id | device.id | CASCADE |
| fk_device_ip_device | device_ip.device_id | device.id | CASCADE |
| fk_project_department | project.department_id | sys_department.id | SET NULL |
| fk_device_project_* | device_project.* | device / project | CASCADE |
| fk_maintenance_device | device_maintenance.device_id | device.id | CASCADE |
| fk_attachment_device | device_attachment.device_id | device.id | CASCADE |
| fk_attachment_upload_user | device_attachment.upload_user_id | sys_user.id | SET NULL |
| fk_status_log_device / _operator | device_status_log.* | device / sys_user | CASCADE / SET NULL |
| fk_operation_log_operator | sys_operation_log.operator_id | sys_user.id | SET NULL |

说明：`device.use_department_id`、`device_attachment.maintenance_id` 在增量脚本中**未**声明物理外键，由应用层保证一致性。

---

## 8. 命名规范

| 类别 | 约定 |
|------|------|
| 表名 | 小写蛇形；系统表 `sys_` 前缀；设备域 `device_` 前缀 |
| 字段 | 小写蛇形；主键 `id`；外键 `*_id`；字典编码 `*_code` |
| 唯一索引 | `uk_*` |
| 普通索引 | `idx_*` |
| 外键 | `fk_*` |

---

## 9. 脚本版本与部署顺序

```text
sql/
├── V1.0.0__init_schema.sql              # 建库 + 18 表 + 外键
├── V1.0.1__init_auth_data.sql           # 默认部门、ADMIN、菜单、登录配置
├── V1.0.2__init_rbac_data.sql           # DEPT_ADMIN / DEVICE_OWNER 及权限、测试账号
├── V1.0.3__init_system_menu.sql         # 系统管理子菜单、按钮权限、示例品牌/类型
├── V1.0.4__device_status_log_remark.sql # status_log.remark
├── V1.0.5__maintenance_enhance.sql      # attachment.maintenance_id + fault_type 字典
├── V1.0.6__device_fields_enhance.sql    # device / config / ip 字段增强
├── V1.0.7__remove_statistics_menu.sql   # 菜单调整（去重统计分析）
└── V1.0.8__device_manager_name.sql      # device.manager_name
```

执行注意：

1. 按版本号顺序执行；客户端使用 `--default-character-set=utf8mb4` 或 `SET NAMES utf8mb4`
2. `CREATE TABLE IF NOT EXISTS` **不会**自动升级已有表结构；旧库必须跑增量 `ALTER`
3. 密码字段需能容纳 BCrypt（约 60 字符），建议 `VARCHAR(255)`

---

