# 第八章 安全设计

## 8.1 安全设计概述

### 8.1.1 设计目标

设备管理系统用于管理机房服务器、交换机、存储、防火墙等重要基础设施，涉及设备资产信息、网络地址、管理账号、运维记录及附件资料等重要数据。因此，系统必须具备完善的安全防护能力，确保数据的**机密性（Confidentiality）**、**完整性（Integrity）**、**可用性（Availability）**。

本系统安全设计遵循以下原则：

1. 最小权限原则（Least Privilege）
2. 深度防御原则（Defense in Depth）
3. 身份可信原则（Identity First）
4. 全过程审计原则（Audit Trail）
5. 数据安全原则（Data Security）
6. 默认安全原则（Secure by Default）
7. 安全与易用性平衡原则

---

# 8.2 系统安全总体架构

系统安全采用多层防护机制，形成纵深防御体系。

```text
                浏览器（HTTPS）
                       │
                       ▼
             身份认证（JWT）
                       │
                       ▼
              权限认证（RBAC）
                       │
                       ▼
            数据权限（部门隔离）
                       │
                       ▼
             参数合法性校验
                       │
                       ▼
              SQL参数化查询
                       │
                       ▼
             MySQL数据库存储
                       │
                       ▼
            操作日志 + 生命周期日志
```

各层安全机制相互独立、相互补充，共同保障系统安全。

---

# 8.3 身份认证设计

## 8.3.1 登录认证

系统采用 **JWT（JSON Web Token）** 进行身份认证。

登录流程如下：

```text
输入用户名密码
       │
       ▼
服务器校验账号密码
       │
       ▼
查询用户角色
       │
       ▼
生成JWT Token
       │
       ▼
返回Token
       │
       ▼
后续请求携带Token
```

登录成功后，客户端在每次请求 Header 中携带：

```http
Authorization: Bearer <JWT Token>
```

服务器验证 Token 后方可访问业务接口。

---

## 8.3.2 Token设计

Token中包含：

* 用户ID
* 用户名
* 部门ID
* 角色信息
* 登录时间
* 过期时间

建议：

| 项目            | 建议值           |
| ------------- | ------------- |
| Token有效期      | 8小时           |
| Refresh Token | 7天            |
| 登录超时          | 自动退出          |
| Token算法       | HS256 或 RS256 |

---

## 8.3.3 密码安全

密码不得明文保存。

采用：

```
BCrypt
```

密码规则建议：

| 项目   | 要求    |
| ---- | ----- |
| 最小长度 | 8位    |
| 最大长度 | 32位   |
| 必须包含 | 字母+数字 |
| 建议包含 | 特殊字符  |

密码修改：

需要验证：

旧密码。

---

## 8.3.4 登录失败控制

为了防止暴力破解。

建议：

连续失败：

5次

↓

锁定：

30分钟

管理员可解除锁定。

同时记录：

* 登录IP
* 浏览器
* 登录时间
* 登录结果

---

# 8.4 权限安全设计

系统采用：

RBAC。

权限包括：

```text
菜单权限

↓

按钮权限

↓

接口权限

↓

数据权限
```

任何业务请求：

必须：

经过：

身份认证

↓

权限认证

↓

数据权限过滤

↓

业务处理

禁止：

绕过权限。

---

## 数据权限

管理员：

查看全部。

部门管理员：

查看本部门。

普通用户：

查看本人负责设备。

SQL自动增加：

```sql
WHERE department_id = 当前部门
```

或者：

```sql
WHERE manager_user_id = 当前用户
```

---

# 8.5 数据安全设计

## 8.5.1 数据唯一性

系统保证：

设备编号

SN号

业务IP

管理IP

唯一。

数据库建立：

UNIQUE INDEX。

保存前：

再次校验。

---

## 8.5.2 数据一致性

所有涉及：

设备新增

设备维修

生命周期

Excel导入

均采用：

数据库事务。

事务失败：

全部回滚。

---

## 8.5.3 数据完整性

设备：

逻辑删除。

维修记录：

永久保存。

生命周期：

永久保存。

日志：

永久保存。

---

# 8.6 数据库安全设计

数据库账号：

禁止：

root连接业务。

建议：

创建：

业务账号。

权限：

仅：

SELECT

INSERT

UPDATE

DELETE

禁止：

DROP

ALTER

CREATE USER

SUPER

等权限。

---

数据库建议：

开启：

慢SQL日志。

开启：

Binlog。

开启：

自动备份。

---

# 8.7 SQL注入防护

所有SQL：

采用：

MyBatis Plus。

禁止：

```java
String sql =
"select * from device where name='"+name+"'";
```

采用：

```java
LambdaQueryWrapper
```

或者：

PreparedStatement。

禁止：

字符串拼接SQL。

---

# 8.8 XSS攻击防护

所有用户输入：

进行过滤。

包括：

备注

维修说明

附件名称

项目名称

部门名称

禁止：

HTML

JavaScript

脚本执行。

输出：

统一HTML转义。

---

# 8.9 文件安全设计

附件上传：

允许：

PDF

Word

Excel

JPG

PNG

ZIP

RAR

禁止：

EXE

BAT

CMD

DLL

JS

SH

等可执行文件。

---

文件大小：

默认：

100MB。

支持：

病毒扫描（后续可扩展）。

上传后：

随机文件名。

数据库：

保存：

UUID。

避免：

文件覆盖。

---

# 8.10 通信安全

建议：

全站HTTPS。

TLS：

1.2以上。

禁止：

HTTP。

登录密码：

加密传输。

Token：

HTTPS传输。

---

# 8.11 日志安全设计

系统记录：

登录日志。

操作日志。

生命周期日志。

日志包括：

操作人

IP

浏览器

时间

URL

请求参数

修改前

修改后

结果

日志：

禁止修改。

禁止删除。

支持：

归档。

---

# 8.12 Excel导入安全

导入前：

检查：

Excel模板。

检查：

文件格式。

检查：

数据合法性。

检查：

设备编号。

检查：

SN。

检查：

IP。

全部通过：

才允许写数据库。

否则：

全部回滚。

---

# 8.13 附件访问安全

附件：

不能：

直接访问。

必须：

登录。

权限：

校验。

例如：

```
GET

/file/download/{id}
```

流程：

```text
登录

↓

权限

↓

设备权限

↓

附件权限

↓

下载
```

避免：

直接访问：

MinIO地址。

---

# 8.14 审计安全设计

所有：

新增。

修改。

删除。

导入。

导出。

上传。

下载。

全部：

记录：

```text
操作人

时间

IP

浏览器

URL

设备

修改前

修改后
```

形成：

完整审计链。

---

# 8.15 安全备份设计

数据库：

每日：

自动备份。

附件：

每日：

增量备份。

日志：

每月：

归档。

建议：

保留：

30天。

支持：

恢复。

---

# 8.16 安全配置要求

| 项目       | 配置要求                    |
| -------- | ----------------------- |
| 登录认证     | JWT + BCrypt            |
| HTTPS    | 必须启用                    |
| Token有效期 | 8小时                     |
| 登录失败锁定   | 连续5次失败锁定30分钟            |
| SQL防护    | 参数化查询（MyBatis Plus）     |
| XSS防护    | 输入过滤 + 输出转义             |
| CSRF防护   | 使用JWT后可关闭传统CSRF，前后端统一鉴权 |
| 文件上传     | 白名单类型、大小限制、随机文件名        |
| 数据删除     | 逻辑删除                    |
| 审计日志     | 永久保留（按单位策略归档）           |
| 数据备份     | 每日自动备份                  |
| 数据库账号    | 最小权限原则                  |
| 权限模型     | RBAC + 数据权限             |
| 数据完整性    | 事务控制 + 唯一约束             |

---

# 8.17 应急与灾难恢复设计

为保证系统在异常情况下能够快速恢复，系统应具备以下应急能力：

1. **数据库故障恢复**

   * 利用每日全量备份和 Binlog 恢复数据。
   * 关键业务数据恢复后应进行一致性校验。

2. **附件恢复**

   * 文件存储采用定期备份策略。
   * 恢复后应校验附件与数据库记录的一致性。

3. **系统故障处理**

   * 应用服务异常退出后支持自动重启。
   * 关键错误写入系统日志，便于定位问题。

4. **安全事件响应**

   * 发现异常登录、越权访问或大量失败请求时，应记录安全事件日志，并支持管理员查看和分析。

---

## 本章总结

本章从身份认证、权限控制、数据安全、数据库安全、通信安全、文件安全、日志审计及灾难恢复等方面，对设备管理系统进行了全面的安全设计。系统采用 **JWT + RBAC + 数据权限 + HTTPS + 参数化查询 + 审计日志** 的整体安全架构，在保障系统易用性的同时，能够有效防范越权访问、SQL 注入、XSS 攻击、数据篡改等常见安全风险，满足设备全生命周期管理对安全性和可追溯性的要求。
