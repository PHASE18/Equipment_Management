# 设备管理系统接口设计说明书

**API Design Specification**

| 项 | 内容 |
|----|------|
| 版本 | 与当前后端 Controllers 一致（含迁移、生命周期方案 A） |
| 风格 | RESTful |
| 数据格式 | JSON；文件上传为 `multipart/form-data` |
| 字符编码 | UTF-8 |
| 认证 | JWT Bearer Token |
| 本地后端 | `http://localhost:8080`，统一前缀 `/api` |

依据：`backend/src/main/java/.../controller`。

---

## 1. 通用约定

### 1.1 URL

```text
/api/{模块}/{资源或动作}
```

### 1.2 HTTP 方法

| 方法 | 用途 |
|------|------|
| GET | 查询 |
| POST | 新增 / 提交动作 |
| PUT | 修改 |
| DELETE | 删除 |

### 1.3 请求头

```http
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json
```

文件上传使用 `multipart/form-data`，仍需 Token。

### 1.4 统一响应 `Result`

```json
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

失败时 `success=false`，`data` 通常为 `null`。

### 1.5 分页

请求（`PageQuery`）：

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| pageNum | long | 1 | 页码 ≥1 |
| pageSize | long | 20 | 每页 1～500 |
| keyword | string | | 关键词（含义因接口而异） |
| sortField | string | createTime | 排序字段 |
| sortOrder | string | desc | asc / desc |

响应 `PageResult`：`records` / `total` / `pageNum` / `pageSize` / `pages`。

### 1.6 BaseCrud 惯例

继承 `BaseCrudController` 的资源默认提供：

| 方法 | 路径 | 权限后缀 |
|------|------|----------|
| GET | `/{module}/page` | `:list` |
| GET | `/{module}/{id}` | `:view` |
| POST | `/{module}` | `:add` |
| PUT | `/{module}` | `:edit` |
| DELETE | `/{module}/{id}` | `:delete` |

类上 `@CrudPermission(module="xxx")` 时，权限码为 `xxx:list|view|add|edit|delete`。  
方法级 `@RequirePermission` 优先于 CRUD 约定。  
超管角色编码（`ADMIN` 等）校验时直接放行。

### 1.7 数据范围

登录后 `user.dataScope`：

| 值 | 角色示例 | 设备可见范围 |
|----|----------|--------------|
| ALL | ADMIN | 全部 |
| DEPARTMENT | DEPT_ADMIN | `department_id` = 用户部门 |
| SELF | DEVICE_OWNER | `manager_user_id` = 用户 ID |

列表、统计、从属表查询自动叠加过滤（见《总体设计》3.6）。

### 1.8 匿名接口

仅 **`POST /api/login`** 可匿名。其余业务接口需登录（`@RequireAuth`）。  
`POST /api/refresh` 允许过期 Token 在宽限期内续期。

---

## 2. 错误码（`ErrorCode`）

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录 |
| 403 | 无权限 |
| 404 | 不存在 |
| 409 | 数据冲突 |
| 1001 | Token 失效 |
| 1002 | 用户名或密码错误 |
| 1003 | 权限不足 |
| 1004 | 设备不存在 |
| 1005 | 设备编号重复 |
| 1006 | SN 重复 |
| 1007 | 业务 IP 重复 |
| 1008 | 管理 IP 重复 |
| 1009 | 项目不存在 |
| 1010 | 维修记录不存在 |
| 1011 | 附件不存在 |
| 1012 | Excel 格式错误 |
| 1013 | Excel 导入失败 |
| 1014 | 生命周期流转非法 |
| 1015 | 数据已删除 |
| 1016 | 账号已锁定 |
| 1099 | 服务器异常（兜底） |

---

## 3. 认证模块

类：`AuthController`。

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/login` | 匿名 | 登录 |
| POST | `/api/refresh` | 登录（可宽限） | Token 续期 |
| GET | `/api/user/info` | 登录 | 当前用户 |
| PUT | `/api/user/password` | 登录 | 修改密码 |
| POST | `/api/logout` | 登录 | 退出 |

### 3.1 登录

**请求**

```json
{ "username": "admin", "password": "123456" }
```

**响应 data**

```json
{
  "token": "eyJ...",
  "user": {
    "id": 1,
    "username": "admin",
    "name": "系统管理员",
    "departmentId": 1,
    "roles": ["ADMIN"],
    "permissions": ["dashboard:view", "device:list", "..."],
    "menus": [{ "id": 1, "title": "首页统计", "path": "/dashboard", "permissionCode": "dashboard:view", "children": [] }],
    "dataScope": "ALL"
  }
}
```

### 3.2 修改密码

```json
{ "oldPassword": "123456", "newPassword": "654321" }
```

---

## 4. 系统管理

### 4.1 用户 `/api/user`

`@CrudPermission(module="system:user")`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/user/list` | system:user:list | 业务分页 |
| GET | `/api/user/page` | system:user:list | BaseCrud 分页 |
| GET | `/api/user/{id}` | system:user:view | 详情 |
| POST | `/api/user` | system:user:add | 新增（BCrypt；忽略残留 id） |
| PUT | `/api/user` | system:user:edit | 修改（空密码不改密） |
| DELETE | `/api/user/{id}` | system:user:delete | 删除 |
| PUT | `/api/user/resetPassword/{id}` | system:user:edit | 重置为 123456 |

### 4.2 角色 `/api/role`

`@CrudPermission(module="system:role")` → 标准 CRUD。  
`roleCode` 唯一，影响数据范围与超管判定。

### 4.3 部门 `/api/department`

`@CrudPermission(module="system:dept")`：`GET /tree` + BaseCrud。

### 4.4 用户角色 `/api/user-role`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/page` | 分页 |
| GET | `/by-user/{userId}` | 已绑角色 ID |
| POST | `/bind` | 绑定 `{ userId, roleIds }` |
| DELETE | `/unbind` | 解绑 |

### 4.5 角色权限 `/api/role-permission`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/page` | 分页 |
| POST | `/bind` | 绑定 |
| DELETE | `/unbind` | 解绑 |

另：`GET /api/menu`、`GET /api/permission`、`POST /api/role/permission`、`POST /api/user/role`（`PermissionController`）。日常绑定优先 `/api/user-role`、`/api/role-permission`。

### 4.6 字典与品牌类型

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/dict/{type}` | 按类型查字典 |
| CRUD | `/api/dict/crud` | 字典通用 CRUD |
| CRUD | `/api/device-brand` | 品牌 `system:brand:*` |
| CRUD | `/api/device-type` | 设备类型 `system:device-type:*` |

### 4.7 系统配置 `/api/config`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/config` | 列表 |
| PUT | `/api/config/batch` | 批量更新 |
| + | BaseCrud | page / id / POST / PUT / DELETE |

---

## 5. 选项接口 `/api/options`

登录即可，**不要求** `system:*`（供表单/筛选下拉）。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/departments` | 部门树 |
| GET | `/brands` | 品牌 |
| GET | `/device-types` | 设备类型 |
| GET | `/users` | 启用用户简表 |

---

## 6. 设备模块

### 6.1 设备档案 `/api/device`

`@CrudPermission(module="device")`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/device/list` | device:list | 业务分页（数据范围） |
| GET | `/api/device/page` | device:list | BaseCrud |
| GET | `/api/device/{id}` | device:view | 详情 |
| POST | `/api/device` | device:add | 新增 |
| PUT | `/api/device` | device:edit | 修改 |
| DELETE | `/api/device/{id}` | device:delete | 删除 |

查询参数含 `keyword`、`status`、`departmentId` 等。  
`status=MAINTAINING` 时按 **`maintaining_flag=1`** 筛选（非主状态码）。  
主体字段见《数据库设计》`device` 表。

### 6.2 生命周期 `/api/device/status`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/list/{deviceId}` | device:view | 分页日志 |
| GET | `/history/{deviceId}` | device:view | 历史列表 |
| GET | `/transitions/{deviceId}` | device:view | 可流转目标主状态 |
| POST | `/change` | device:edit | 主状态流转 |

**主状态流转矩阵**

```text
PURCHASING → IN_STOCK → PENDING_ONLINE → IN_USE
IN_USE  → STOPPED | OFFLINE
STOPPED → IN_USE | OFFLINE
OFFLINE → IN_USE | SCRAPPED
SCRAPPED 终态
```

废弃主状态：`STANDBY`、`MAINTAINING`（维修用 `maintaining_flag`）。

**change 请求要点**：`deviceId`、`toStatus`（或等价字段）、`reason` 必填；可选 `fromStatus` 乐观校验。  
非法流转 → `1014`。响应含 `maintainingFlag` 与完整 history。

维修标志由维修模块开/关，不通过本接口改主状态为「维修中」。

### 6.3 设备迁移 `/api/device/migrate`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/device/migrate` | device:edit | 执行迁移 |
| GET | `/api/device/migrate/history/{deviceId}` | device:view | 迁移历史 |
| GET | `/api/device/migrate/{id}` | device:view | 迁移明细（含 items） |

**迁移请求（`DeviceMigrateRequest`）要点**

| 字段 | 说明 |
|------|------|
| deviceId | 必填 |
| reason | 必填 |
| projectIds | 非 null 时全量同步所属项目 |
| 档案/网络/配置字段 | 前端回填当前值后提交；后端 diff 后只更新有变更项 |

无任何字段变更 → 业务错误（如 400）。  
结果：`migrationId`、`changedFieldCount`、`items[]`（fieldGroup / fieldKey / fieldLabel / oldValue / newValue）。

### 6.4 IP / 网络 `/api/ip`

`@CrudPermission(module="device")`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/device/{deviceId}` | 按设备取一条 |
| GET | `/list/{deviceId}` | 列表 |
| POST | `/save` | 按设备 upsert |

业务 IP / 管理 IP 冲突 → `1007` / `1008`。

### 6.5 设备配置 `/api/device-config`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/device/{deviceId}` | 查询 |
| POST | `/save` | 保存 |

---

## 7. 项目模块

### 7.1 项目 `/api/project`

`@CrudPermission(module="project")`：`GET /list` + BaseCrud + `POST /bindDevice` + `DELETE /unbind`。

### 7.2 设备-项目 `/api/device-project`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/page` | 分页 |
| GET | `/by-device/{deviceId}` | 项目 ID 列表 |
| POST | `/sync` | 全量同步 `{ deviceId, projectIds }` |
| POST | `/bind` | 绑定 |
| DELETE | `/unbind` | 解绑 |

---

## 8. 维修模块 `/api/maintenance`

`@CrudPermission(module="maintenance")`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/list` | 分页 VO |
| GET | `/detail/{id}` | 详情 VO |
| GET | `/fault-stats` | 故障类型统计 |
| POST | `/`、`/submit` | 创建（仅在用/停用可开单；置 `maintaining_flag=1`） |
| PUT | `/` | 更新 |
| PUT | `/{id}/complete` | 完成（无未结工单时清标志） |
| DELETE | `/{id}` | 删除 |
| + | BaseCrud page/id | |

列表受设备数据范围约束。

---

## 9. 附件与文件

### 9.1 文件 `/api/file`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/upload` | attachment:upload | 单文件 |
| POST | `/batch-upload` | attachment:upload | 批量 |
| GET | `/list/{deviceId}` | list 或 download | 设备附件 |
| GET | `/list/maintenance/{maintenanceId}` | list 或 download | 维修附件 |
| GET | `/download/{id}` | attachment:download | 下载流 |
| DELETE | `/{id}` | attachment:delete | 删除 |

**upload 表单**：`file`（必填）、`deviceId`（必填）、`category`（image/document/excel/contract）、`fileTypeCode`、`maintenanceId`。

### 9.2 附件元数据 `/api/attachment`

`@CrudPermission(module="attachment")` → 标准 CRUD。

---

## 10. Excel `/api/excel`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/import` | device:import | 导入 |
| GET | `/export` | device:export | 导出（当前筛选 + 数据范围） |
| GET | `/template` | device:export | 模板 |

---

## 11. 统计分析 `/api/statistics`

类权限：`dashboard:view`。结果按数据范围过滤。

| 路径 | 说明 |
|------|------|
| `/dashboard`、`/home` | 首页聚合 / 摘要（含 maintainingCount=维修标志计数） |
| `/brand`、`/type`、`/status`、`/department`、`/project` | 分布 |
| `/fault`、`/faultRank`、`/maintenance-trend`、`/cost` | 故障与维修 |
| `/warranty`、`/supplier`、`/maintenance-company`、`/model`、`/scrap` | 其它维度 |

---

## 12. 日志

### 12.1 聚合 `/api/log`

权限：`log:view` 或 `log:list`。

| 路径 | 说明 |
|------|------|
| GET `/login` | 登录日志 |
| GET `/operation` | 操作日志 |
| GET `/status` | 生命周期日志 |

### 12.2 实体分页

同等日志权限：`/api/login-log`、`/api/operation-log`、`/api/device-status-log`（page / {id}；删除以服务端规则为准）。

---

## 13. API 权限矩阵（种子角色）

| 能力 | ADMIN | DEPT_ADMIN | DEVICE_OWNER |
|------|:-----:|:----------:|:------------:|
| 登录 / 改密 / 刷新 | ✓ | ✓ | ✓ |
| 首页 dashboard:view | ✓ | ✓ | ✓ |
| 系统管理 system:* | ✓ | ✗ | ✗ |
| 设备查询 | 全部 | 本部门 | 本人负责 |
| 设备编辑 / 迁移 / 流转 | ✓ | ✓（范围） | 视权限码与范围 |
| 设备删除 / 导入 | ✓ | 视种子 | 通常无 |
| 设备导出 | ✓ | ✓ | ✓ |
| 项目 | ✓ | ✓ | 视种子 |
| 维修 | ✓ | ✓ | 本人范围 |
| 附件 | ✓ | ✓ | 上传下载为主 |
| 日志 | ✓ | ✓ | ✗ |

精确以库中 `sys_role_permission` 为准。

---

## 14. 联调说明

1. 前端 Vite 将 `/api` 代理到 `8080`。  
2. 先登录取 Token，再带 `Authorization: Bearer ...`。  
3. 菜单来自 `menus`，勿写死无权限路由。  
4. 下拉优先 `/api/options/*`，避免部门管理员调 `system:*` 得 1003。  
5. 未捕获异常统一 `code=1099`。  
6. 迁移/生命周期冒烟脚本：`scripts/test-device-migrate-api.ps1`、`scripts/test-lifecycle-scheme-a.ps1`。

---

## 15. 附录：模块与 Controller

| 模块 | Controller |
|------|------------|
| 认证 | AuthController |
| 用户 / 角色 / 部门 | SysUserController / SysRoleController / SysDepartmentController |
| 用户角色 / 角色权限 | SysUserRoleController / SysRolePermissionController / PermissionController |
| 字典 / 品牌 / 类型 | DictController / DictCrudController / DeviceBrandController / DeviceTypeController |
| 配置 / 选项 | SysConfigController / OptionsController |
| 设备 / 状态 / 迁移 | DeviceController / DeviceStatusController / DeviceMigrateController |
| IP / 配置 | DeviceIpController / DeviceConfigController |
| 项目 | ProjectController / DeviceProjectController |
| 维修 | DeviceMaintenanceController |
| 文件 | FileController / DeviceAttachmentController |
| Excel / 统计 | ExcelController / StatisticsController |
| 日志 | LogController / SysLoginLogController / SysOperationLogController / DeviceStatusLogController |
