**《设备管理系统接口设计说明书（API）》V1.0**，与前面的**SRS、数据库设计**完全对应，按照企业项目（Spring Boot + Vue3 + RESTful API）标准编写

## 1. 文档应保留的通用约定（与实现一致）

| 项 | 实际 |
|----|------|
| 前缀 | `/api/...` |
| 认证 | `Authorization: Bearer <JWT>` |
| 返回 | `{ code, message, data }`（`Result`） |
| 分页 | `pageNum` / `pageSize` |
| 分页结果 | `{ records, total, pageNum, pageSize }` |
| 成功码 | `200` |
| 业务错误码 | 与 `ErrorCode.java` 一致：`1001–1016`、`1099` 等 |
| CRUD 惯例 | 多数资源继承 `BaseCrudController`：`GET /page`、`GET /{id}`、`POST`、`PUT`、`DELETE /{id}` |
| 权限 | 类上 `@CrudPermission(module)` → `{module}:list/view/add/edit/delete`，或 `@RequirePermission` |
| 数据范围 | `ADMIN`→全部，`DEPT_ADMIN`→本部门，`DEVICE_OWNER`→本人 |

本地开发实际是 HTTP（如 `8080`）

---

## 2. 按模块应列出的接口清单（与代码对齐）

### 认证
| 方法 | 路径 |
|------|------|
| POST | `/api/login` |
| POST | `/api/refresh`（文档缺） |
| GET | `/api/user/info` |
| PUT | `/api/user/password` |
| POST | `/api/logout` |

登录 `data`：`token` + `user`（含 `roles`、`permissions`、`menus`、`dataScope`）。

### 用户 / 角色 / 部门
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/user/list` | 业务列表 |
| CRUD | `/api/user`（含 `/page`、`/{id}`） | |
| PUT | `/api/user/resetPassword/{id}` | |
| CRUD | `/api/role` | |
| GET | `/api/department/tree` | |
| CRUD | `/api/department` | |
| GET | `/api/user-role/by-user/{userId}` | |
| POST/DELETE | `/api/user-role/bind`、`/unbind` | |
| GET/POST/DELETE | `/api/role-permission/page`、`/bind`、`/unbind` | |

### 选项
| GET | `/api/options/departments` |
| GET | `/api/options/brands` |
| GET | `/api/options/device-types` |
| GET | `/api/options/users` |

### 字典 / 品牌 / 类型
| GET | `/api/dict/{type}` |
| CRUD | `/api/dict/crud`、`/api/device-brand`、`/api/device-type` |
| CRUD | `/api/permission/crud`（若启用） |

### 设备
| GET | `/api/device/list`、`/page`、`/{id}` |
| POST/PUT/DELETE | `/api/device`、`/api/device/{id}` |
| GET | `/api/device/status/list/{deviceId}`、`/history/{deviceId}`、`/transitions/{deviceId}` |
| POST | `/api/device/status/change` |
| GET | `/api/ip/list/{deviceId}`、`/device/{deviceId}` |
| POST | `/api/ip/save` |
| GET/POST | `/api/device-config/device/{deviceId}`、`/save` |

### 项目 / 设备-项目
| GET/CRUD | `/api/project/list` + CRUD |
| POST/DELETE | `/api/project/bindDevice`、`/unbind` |
| | `/api/device-project/page`、`/by-device/{id}`、`/sync`、`/bind`、`/unbind` |

### 维修
| GET | `/api/maintenance/list`、`/detail/{id}`、`/fault-stats` |
| POST | `/api/maintenance`、`/submit` |
| PUT | `/api/maintenance`、`/{id}/complete` |
| DELETE | 若走 BaseCrud 则有 `DELETE /{id}` |

### 附件 / 文件
| POST | `/api/file/upload`、`/batch-upload` |
| GET | `/api/file/list/{deviceId}`、`/list/maintenance/{id}`、`/download/{id}` |
| DELETE | `/api/file/{id}` |
| CRUD | `/api/attachment`（元数据） |

### Excel
| POST | `/api/excel/import` |
| GET | `/api/excel/export`、`/template` |

### 统计
| GET | `/api/statistics/dashboard`、`/home` |
| | `/brand`、`/type`、`/status`、`/department`、`/project` |
| | `/fault`、`/faultRank`、`/maintenance-trend`、`/cost` |
| | `/warranty`、`/supplier`、`/maintenance-company`、`/model`、`/scrap` |

### 日志 / 配置 / 权限树
| GET | `/api/log/login`、`/operation`、`/status` |
| | `/api/login-log/page`、`/operation-log/page`、`/device-status-log/page` 等 |
| GET/PUT | `/api/config`、`/api/config/batch` |
| GET | `/api/menu`、`/api/permission` |
| POST | `/api/role/permission`、`/api/user/role`（部分为占位/与 user-role 并存） |

---
