# 设备管理系统接口设计说明书

**API Design Specification**

| 项 | 内容 |
|----|------|
| 版本 | 与当前后端代码一致 |
| 风格 | RESTful |
| 数据格式 | JSON（文件上传为 `multipart/form-data`） |
| 字符编码 | UTF-8 |
| 认证 | JWT Bearer Token |
| 后端端口（本地） | `8080`，前缀 `/api` |

依据 `backend/.../controller` 。

---

## 1. 通用约定

### 1.1 URL

```text
/api/{模块}/{资源或动作}
```

### 1.2 HTTP 方法

| 方法 | 用途 |
|------|------|
| GET | 查询 |
| POST | 新增 / 提交动作 |
| PUT | 修改 |
| DELETE | 删除 |

### 1.3 请求头

```http
Authorization: Bearer <JWT_TOKEN>
Content-Type: application/json
```

文件上传接口使用 `multipart/form-data`，仍需携带 Token。

### 1.4 统一响应 `Result`

```json
{
  "success": true,
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

失败时 `success=false`，`data` 通常为 `null`。

### 1.5 分页

请求参数（`PageQuery`）：

| 参数 | 类型 | 默认 | 说明 |
|------|------|------|------|
| pageNum | long | 1 | 页码，≥1 |
| pageSize | long | 20 | 每页条数，1～500 |
| keyword | string | | 关键词（各接口含义不同） |
| sortField | string | createTime | 排序字段 |
| sortOrder | string | desc | asc / desc |

响应 `PageResult`：

```json
{
  "records": [],
  "total": 0,
  "pageNum": 1,
  "pageSize": 20,
  "pages": 0
}
```

### 1.6 BaseCrud 惯例

继承 `BaseCrudController` 的资源默认提供：

| 方法 | 路径 | 权限后缀（见下） |
|------|------|------------------|
| GET | `/{module}/page` | `:list` |
| GET | `/{module}/{id}` | `:view` |
| POST | `/{module}` | `:add` |
| PUT | `/{module}` | `:edit` |
| DELETE | `/{module}/{id}` | `:delete` |

类上 `@CrudPermission(module = "xxx")` 时，所需权限码为 `xxx:list` / `xxx:view` / `xxx:add` / `xxx:edit` / `xxx:delete`。  
方法名或 HTTP 动词会映射到上述后缀（如 `bind`→`add`，`unbind`→`delete`，`change`→`add`）。

`ADMIN` 等超管角色编码在校验时直接放行。

### 1.7 数据范围

登录后 `user.dataScope`：

| 值 | 角色编码示例 | 设备可见范围 |
|----|--------------|--------------|
| ALL | ADMIN | 全部 |
| DEPARTMENT | DEPT_ADMIN | `device.department_id` = 用户部门 |
| SELF | DEVICE_OWNER | `device.manager_user_id` = 用户 ID |

统计、列表等查询会自动叠加数据范围过滤。

### 1.8 匿名接口

仅 `@Anonymous` 方法可不登录访问：当前为 **`POST /api/login`**。  
其余业务接口需登录（`@RequireAuth`）。

---

## 2. 错误码（`ErrorCode`）

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录 |
| 403 | 无权限 |
| 404 | 不存在 |
| 409 | 数据冲突 |
| 500 | 服务器异常（旧码，少用） |
| 1001 | Token 失效 |
| 1002 | 用户名或密码错误 |
| 1003 | 权限不足 |
| 1004 | 设备不存在 |
| 1005 | 设备编号重复 |
| 1006 | SN 重复 |
| 1007 | 业务 IP 重复 |
| 1008 | 管理 IP 重复 |
| 1009 | 项目不存在 |
| 1010 | 维修记录不存在 |
| 1011 | 附件不存在 |
| 1012 | Excel 格式错误 |
| 1013 | Excel 导入失败 |
| 1014 | 生命周期流转非法 |
| 1015 | 数据已删除 |
| 1016 | 账号已锁定 |
| 1099 | 服务器异常（未捕获异常兜底） |

---

## 3. 认证模块

基路径：无统一前缀（直接挂在 `/api`）。类：`AuthController`。

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/login` | 匿名 | 登录 |
| POST | `/api/refresh` | 登录 | Token 续期 |
| GET | `/api/user/info` | 登录 | 当前用户 |
| PUT | `/api/user/password` | 登录 | 修改密码 |
| POST | `/api/logout` | 登录 | 退出 |

### 3.1 登录

**请求**

```json
{
  "username": "admin",
  "password": "123456"
}
```

**响应 data（`LoginResponse`）**

```json
{
  "token": "eyJ...",
  "user": {
    "id": 1,
    "name": "系统管理员",
    "username": "admin",
    "departmentId": 1,
    "department": "信息科技处",
    "roles": ["ADMIN"],
    "permissions": ["dashboard:view", "device:list", "..."],
    "menus": [{ "id": 1, "title": "首页统计", "path": "/dashboard", "permissionCode": "dashboard:view", "children": [] }],
    "dataScope": "ALL"
  }
}
```

### 3.2 修改密码

```json
{
  "oldPassword": "123456",
  "newPassword": "654321"
}
```

---

## 4. 系统管理

### 4.1 用户 `/api/user`

`@CrudPermission(module = "system:user")`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/user/list` | system:user:list | 业务分页（keyword / departmentId） |
| GET | `/api/user/page` | system:user:list | BaseCrud 分页 |
| GET | `/api/user/{id}` | system:user:view | 详情 |
| POST | `/api/user` | system:user:add | 新增（密码 BCrypt；忽略客户端残留 id） |
| PUT | `/api/user` | system:user:edit | 修改（空密码不改密） |
| DELETE | `/api/user/{id}` | system:user:delete | 删除 |
| PUT | `/api/user/resetPassword/{id}` | system:user:edit | 重置为默认密码 123456 |

### 4.2 角色 `/api/role`

`@CrudPermission(module = "system:role")` → 标准 CRUD（`/page`、`/{id}`、POST、PUT、DELETE）。

`roleCode` 唯一；影响数据范围（见 1.7）。角色-菜单权限需写 `sys_role_permission`（页面分配能力有限，见角色权限接口）。

### 4.3 部门 `/api/department`

`@CrudPermission(module = "system:dept")`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/department/tree` | 部门树 |
| + | BaseCrud | page / id / POST / PUT / DELETE |

### 4.4 用户角色 `/api/user-role`

登录即可（类级仅 `@RequireAuth`；绑定动作按方法名映射权限时需注意前端调用场景）。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/user-role/page` | 分页 |
| GET | `/api/user-role/by-user/{userId}` | 用户已绑角色 ID 列表 |
| POST | `/api/user-role/bind` | 绑定 |
| DELETE | `/api/user-role/unbind` | 解绑 |

**bind / unbind 请求体**

```json
{
  "userId": 4,
  "roleIds": [3]
}
```

### 4.5 角色权限 `/api/role-permission`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/role-permission/page` | 分页 |
| POST | `/api/role-permission/bind` | 绑定 |
| DELETE | `/api/role-permission/unbind` | 解绑 |

另：`POST /api/role/permission`、`POST /api/user/role`（`PermissionController`）为权限服务入口，部分方法实现可能为占位，日常绑定优先用 `/api/user-role`、`/api/role-permission`。

### 4.6 菜单与权限树

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/menu` | 当前用户菜单树 |
| GET | `/api/permission` | 权限树 |
| CRUD | `/api/permission/crud` | 权限实体 CRUD |

### 4.7 字典

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/dict/{type}` | 按类型查字典项（如 `device_brand`） |
| CRUD | `/api/dict/crud` | 字典通用 CRUD |
| CRUD | `/api/device-brand` | 品牌（dict_type 固定）`system:brand:*` |
| CRUD | `/api/device-type` | 设备类型 `system:device-type:*` |

### 4.8 系统配置 `/api/config`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/config` | 配置列表 |
| PUT | `/api/config/batch` | 批量更新 |

---

## 5. 选项接口 `/api/options`

登录即可，**不要求** `system:*` 管理权限（供设备表单、筛选下拉使用）。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/options/departments` | 部门树 |
| GET | `/api/options/brands` | 品牌字典 |
| GET | `/api/options/device-types` | 设备类型字典 |
| GET | `/api/options/users` | 启用用户简表（id/username/realName/departmentId） |

---

## 6. 设备模块

### 6.1 设备档案 `/api/device`

`@CrudPermission(module = "device")`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/device/list` | device:list | 业务分页查询（受数据范围） |
| GET | `/api/device/page` | device:list | BaseCrud |
| GET | `/api/device/{id}` | device:view | 详情 |
| POST | `/api/device` | device:add | 新增 |
| PUT | `/api/device` | device:edit | 修改 |
| DELETE | `/api/device/{id}` | device:delete | 删除 |

主体字段见《数据库设计》`device` 表；责任人文本 `managerName`，SELF 仍依赖 `managerUserId`。

### 6.2 生命周期 `/api/device/status`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| GET | `/api/device/status/list/{deviceId}` | device:view | 状态列表 |
| GET | `/api/device/status/history/{deviceId}` | device:view | 变更历史 |
| GET | `/api/device/status/transitions/{deviceId}` | device:view | 可流转目标 |
| POST | `/api/device/status/change` | device:edit | 状态流转 |

**change 请求体（`DeviceStatusChangeRequest`）**：含设备 ID、目标状态、原因等（以 DTO 字段为准）。非法流转返回 `1014`。

### 6.3 IP / 网络 `/api/ip`

`@CrudPermission(module = "device")`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/ip/list/{deviceId}` | 列表 |
| GET | `/api/ip/device/{deviceId}` | 按设备取一条 |
| POST | `/api/ip/save` | 保存（按设备 upsert） |

业务 IP / 管理 IP 冲突分别返回 `1007` / `1008`。

### 6.4 设备配置 `/api/device-config`

`@CrudPermission(module = "device")`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/device-config/device/{deviceId}` | 查询 |
| POST | `/api/device-config/save` | 保存 |

---

## 7. 项目模块

### 7.1 项目 `/api/project`

`@CrudPermission(module = "project")`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/project/list` | 分页列表 |
| + | BaseCrud | page / id / POST / PUT / DELETE |
| POST | `/api/project/bindDevice` | 绑定设备 |
| DELETE | `/api/project/unbind` | 解绑 |

### 7.2 设备-项目 `/api/device-project`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/device-project/page` | 分页 |
| GET | `/api/device-project/by-device/{deviceId}` | 设备已绑项目 |
| POST | `/api/device-project/sync` | 同步绑定集合 |
| POST | `/api/device-project/bind` | 绑定 |
| DELETE | `/api/device-project/unbind` | 解绑 |

---

## 8. 维修模块 `/api/maintenance`

`@CrudPermission(module = "maintenance")`

| 方法 | 路径 | 权限倾向 | 说明 |
|------|------|----------|------|
| GET | `/api/maintenance/list` | list | 分页（含设备信息 VO） |
| GET | `/api/maintenance/page` | list | BaseCrud |
| GET | `/api/maintenance/detail/{id}` | view | 详情 VO |
| GET | `/api/maintenance/fault-stats` | list | 故障类型统计 |
| POST | `/api/maintenance` | add | 创建工单 |
| POST | `/api/maintenance/submit` | add | 创建并返回新 ID |
| PUT | `/api/maintenance` | edit | 更新 |
| PUT | `/api/maintenance/{id}/complete` | edit | 完成工单 |
| DELETE | `/api/maintenance/{id}` | delete | 删除 |

列表查询受设备数据范围约束。

---

## 9. 附件与文件

### 9.1 文件 `/api/file`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/file/upload` | attachment:upload | 单文件上传 |
| POST | `/api/file/batch-upload` | attachment:upload | 批量上传 |
| GET | `/api/file/list/{deviceId}` | list 或 download | 设备附件列表 |
| GET | `/api/file/list/maintenance/{maintenanceId}` | list 或 download | 维修附件列表 |
| GET | `/api/file/download/{id}` | attachment:download | 下载（流） |
| DELETE | `/api/file/{id}` | attachment:delete | 删除 |

**upload 表单字段**

| 字段 | 必填 | 说明 |
|------|------|------|
| file | 是 | 文件 |
| deviceId | 是 | 设备 ID |
| category | 否 | `image` / `document` / `excel` / `contract`，默认 document |
| fileTypeCode | 否 | 业务类型编码；空则按 category 默认 |
| maintenanceId | 否 | 关联维修工单 |

### 9.2 附件元数据 CRUD `/api/attachment`

`@CrudPermission(module = "attachment")` → 标准 page / id / POST / PUT / DELETE。

---

## 10. Excel `/api/excel`

| 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|
| POST | `/api/excel/import` | device:import | 导入设备 |
| GET | `/api/excel/export` | device:export | 导出（当前查询范围） |
| GET | `/api/excel/template` | device:export | 下载模板 |

导出/导入受数据范围影响。

---

## 11. 统计分析 `/api/statistics`

`@RequirePermission("dashboard:view")`，登录用户具备首页权限即可访问全部统计子接口；结果按数据范围过滤。

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/statistics/dashboard` | 首页仪表盘聚合 |
| GET | `/api/statistics/home` | 首页摘要 |
| GET | `/api/statistics/brand` | 品牌分布 |
| GET | `/api/statistics/type` | 类型分布 |
| GET | `/api/statistics/status` | 状态分布 |
| GET | `/api/statistics/department` | 部门分布 |
| GET | `/api/statistics/project` | 项目分布 |
| GET | `/api/statistics/fault` | 故障类型 |
| GET | `/api/statistics/faultRank` | 故障排行 |
| GET | `/api/statistics/maintenance-trend` | 维修趋势 |
| GET | `/api/statistics/cost` | 维修费用 |
| GET | `/api/statistics/warranty` | 到保相关 |
| GET | `/api/statistics/supplier` | 供应商 |
| GET | `/api/statistics/maintenance-company` | 维保单位 |
| GET | `/api/statistics/model` | 型号 |
| GET | `/api/statistics/scrap` | 报废 |

查询参数以各接口实现为准（通常含时间范围等可选过滤）。

---

## 12. 日志

### 12.1 聚合 `/api/log`

`@RequirePermission(any = {"log:view", "log:list"})`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/log/login` | 登录日志 |
| GET | `/api/log/operation` | 操作日志 |
| GET | `/api/log/status` | 生命周期日志 |

### 12.2 实体分页

同等日志权限：

| 前缀 | 接口 |
|------|------|
| `/api/login-log` | page / {id} / DELETE |
| `/api/operation-log` | page / {id} / DELETE |
| `/api/device-status-log` | page / {id} / DELETE |

部分删除接口可能业务禁止物理删（以服务端校验为准）。

---

## 13. API 权限矩阵（种子角色）

| 能力 | ADMIN | DEPT_ADMIN | DEVICE_OWNER |
|------|:-----:|:----------:|:------------:|
| 登录 / 改密 / 刷新 Token | ✓ | ✓ | ✓ |
| 首页统计 dashboard:view | ✓ | ✓ | ✓ |
| 用户/角色/部门 system:* | ✓ | ✗ | ✗ |
| 品牌/类型管理 | ✓ | ✗ | ✗ |
| 设备查询 | 全部 | 本部门 | 本人负责（manager_user_id） |
| 设备新增/编辑 | ✓ | ✓ | 编辑本人；新增视权限码 |
| 设备删除 / 导入 | ✓ | ✓（导入有）/ 删视种子 | 无删除、无导入 |
| 设备导出 | ✓ | ✓ | ✓ |
| 项目 | ✓ | ✓ | 无 project:list（种子） |
| 维修 | ✓ | ✓ | 查看/新增/编辑本人范围 |
| 附件上传下载 | ✓ | ✓ | ✓（无删除种子） |
| 日志 | ✓ | ✓ | ✗ |

说明：精确以 `sys_role_permission` 与 `V1.0.2` 初始化数据为准；自定义角色需自行绑定权限码，否则菜单为空。

---

## 14. 联调说明

1. 本地后端：`http://localhost:8080`；前端 Vite 代理 `/api` → 后端。  
2. 先 `POST /api/login` 取 `token`，后续请求头带 `Authorization: Bearer ...`。  
3. 前端菜单来自登录/`/api/user/info` 的 `menus`，路由按 `permissionCode` 动态注册；无菜单时勿写死跳转 `/dashboard`。  
4. 选项类下拉优先调 `/api/options/*`，避免部门管理员因缺少 `system:*` 而 1003。  
5. 未捕获异常统一 `code=1099 message=服务器异常`，排查需看服务端日志。

---

## 15. 附录：模块与 Controller 对照

| 模块 | Controller |
|------|------------|
| 认证 | AuthController |
| 用户 | SysUserController |
| 角色 | SysRoleController |
| 部门 | SysDepartmentController |
| 用户角色 | SysUserRoleController |
| 角色权限 | SysRolePermissionController / PermissionController |
| 字典/品牌/类型 | DictController / DictCrudController / DeviceBrandController / DeviceTypeController |
| 配置 | SysConfigController |
| 选项 | OptionsController |
| 设备 | DeviceController |
| 状态 | DeviceStatusController |
| IP | DeviceIpController |
| 配置信息 | DeviceConfigController |
| 项目 | ProjectController / DeviceProjectController |
| 维修 | DeviceMaintenanceController |
| 文件 | FileController / DeviceAttachmentController |
| Excel | ExcelController |
| 统计 | StatisticsController |
| 日志 | LogController / SysLoginLogController / SysOperationLogController / DeviceStatusLogController |
