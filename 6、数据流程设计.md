# 第六章 数据流程设计

## 6.1 数据流程设计概述

### 6.1.1 设计目标

数据流程设计（Data Flow Design）用于描述系统数据在各业务模块之间的流转过程，明确数据来源、处理过程、存储方式及输出结果，为数据库设计、接口设计及系统开发提供依据。

本系统以**设备档案（Device）**作为核心数据对象，围绕设备生命周期构建设备管理业务，实现设备信息、项目管理、维修管理、附件管理、统计分析及日志审计等业务的数据流转。

系统遵循以下原则：

1. **设备档案唯一原则**：所有业务数据均围绕唯一设备建立关联。
2. **全过程留痕原则**：所有关键业务操作均记录日志，实现全过程可追溯。
3. **数据一致性原则**：业务数据与统计数据保持实时一致。
4. **数据完整性原则**：设备删除采用逻辑删除，不直接物理删除。
5. **实时更新原则**：设备状态、维修记录、统计信息实时同步更新。

---

# 6.2 系统总体数据流

系统总体数据流如下：

```text
                    Excel导入
                        │
                        ▼
                 设备基础档案(device)
                        │
        ┌───────────────┼────────────────┐
        │               │                │
        ▼               ▼                ▼
   配置信息       网络IP信息        项目关联信息
(device_config) (device_ip)    (device_project)
        │               │                │
        └───────────────┼────────────────┘
                        ▼
                 生命周期管理
             (device_status_log)
                        │
        ┌───────────────┴───────────────┐
        ▼                               ▼
   维修管理                     附件管理
(device_maintenance)      (device_attachment)
        │                               │
        └───────────────┬───────────────┘
                        ▼
                  操作日志记录
             (sys_operation_log)
                        │
                        ▼
                  统计分析模块
                        │
                        ▼
                  首页可视化展示
```

系统所有业务均以设备档案为中心展开。

---

# 6.3 登录认证数据流程

## 6.3.1 流程说明

用户输入账号和密码后，系统完成身份认证，并根据角色加载权限及菜单。

数据流程如下：

```text
用户输入账号密码
        │
        ▼
身份认证
        │
        ▼
查询用户信息(sys_user)
        │
        ▼
密码校验
        │
        ▼
查询角色(sys_role)
        │
        ▼
查询权限(sys_permission)
        │
        ▼
生成JWT Token
        │
        ▼
返回前端
```

登录成功后：

* 更新最后登录时间；
* 记录登录日志；
* 返回用户信息及菜单权限。

登录失败：

* 增加失败次数；
* 超过限制后锁定账号（可配置）。

---

# 6.4 设备档案数据流程

设备档案是整个系统的数据核心。

## 6.4.1 新增设备

```text
填写设备信息
        │
        ▼
字段合法性校验
        │
        ▼
唯一性检查
 ├──设备编号
 ├──SN号
 └──IP地址
        │
        ▼
写入设备表(device)
        │
        ├────►写入配置表
        ├────►写入IP表
        ├────►写入项目关联
        └────►写入附件
        │
        ▼
生成生命周期记录
        │
        ▼
记录操作日志
```

系统自动生成：

* 生命周期记录
* 操作日志

---

## 6.4.2 修改设备

```text
打开设备详情
        │
        ▼
修改设备信息
        │
        ▼
字段校验
        │
        ▼
更新数据库
        │
        ▼
记录修改前数据
        │
        ▼
记录修改后数据
        │
        ▼
写入操作日志
```

所有修改必须记录：

* 修改字段
* 修改前内容
* 修改后内容
* 修改人
* 修改时间

---

## 6.4.3 删除设备

设备采用**逻辑删除**。

```text
删除设备
      │
      ▼
状态检查
      │
      ▼
逻辑删除
(deleted=1)
      │
      ▼
写入日志
```

系统不允许物理删除设备数据。

---

# 6.5 生命周期数据流程

设备生命周期如下：

```text
采购中
    │
    ▼
库存
    │
    ▼
待上架
    │
    ▼
在用
 ┌──┴────────┐
 ▼           ▼
维修中      备用
 │           │
 └────┬──────┘
      ▼
     在用
      │
      ▼
     停用
      │
      ▼
     报废
```

每次状态变化：

```text
修改状态
      │
      ▼
检查状态是否合法
      │
      ▼
更新device.status
      │
      ▼
新增device_status_log
      │
      ▼
记录操作日志
```

生命周期日志永久保存。

---

# 6.6 IP地址管理数据流程

系统支持：

业务IP

管理IP

新增IP：

```text
输入IP
      │
      ▼
格式校验
      │
      ▼
唯一性检查
      │
      ▼
保存device_ip
      │
      ▼
写入日志
```

若IP重复：

立即提示：

> "IP地址已存在，不允许重复。"

系统终止保存。

---

# 6.7 项目关联数据流程

一个设备：

多个项目。

一个项目：

多个设备。

```text
选择设备
      │
      ▼
选择项目
      │
      ▼
写入device_project
      │
      ▼
记录日志
```

解绑：

删除关联记录。

设备档案不受影响。

---

# 6.8 维修管理数据流程

维修业务流程如下：

```text
设备故障
      │
      ▼
新增维修记录
      │
      ▼
设备状态：
在用
 │
 ▼
维修中
      │
      ▼
维修完成
      │
      ▼
填写：
维修日期
维修人员
维修单位
故障原因
故障类型
更换部件
维修费用
恢复日期
是否解决
      │
      ▼
状态恢复：
维修中
 │
 ▼
在用
      │
      ▼
更新统计数据
      │
      ▼
记录日志
```

维修记录永久保存，不允许删除历史数据（仅可作废或补充说明）。

---

# 6.9 附件管理数据流程

附件上传流程：

```text
选择文件
      │
      ▼
文件大小校验
      │
      ▼
文件格式校验
      │
      ▼
上传MinIO
      │
      ▼
保存附件信息
(device_attachment)
      │
      ▼
记录操作日志
```

支持附件类型：

* 设备照片
* 采购合同
* 验收报告
* 维修报告
* 配置截图
* 维保合同
* 机柜照片
* 其他附件

删除附件：

仅删除附件记录。

原文件可根据系统配置决定是否立即删除。

---

# 6.10 Excel导入数据流程

历史设备导入流程：

```text
上传Excel
      │
      ▼
解析Excel
      │
      ▼
逐行校验
 ├──设备编号
 ├──SN号
 ├──IP
 ├──品牌
 ├──类型
 ├──部门
 └──项目
      │
      ▼
生成错误报告
      │
      ▼
全部通过？
 ├────否────►返回错误信息
 │
 ▼
写入数据库
      │
      ▼
生成导入日志
```

导入结束后：

生成：

* 成功数量
* 失败数量
* 错误原因

支持错误数据导出。

---

# 6.11 统计分析数据流程

统计数据来源：

```text
device
device_config
device_ip
device_project
device_maintenance
device_status_log
```

统计流程：

```text
数据库查询
      │
      ▼
SQL统计
      │
      ▼
生成JSON
      │
      ▼
ECharts展示
```

统计数据包括：

* 品牌统计
* 类型统计
* 状态统计
* 项目统计
* 故障统计
* 维修费用
* 更换部件排行
* 型号排行

---

# 6.12 日志数据流程

所有关键操作均记录日志。

```text
业务操作
      │
      ▼
获取旧数据
      │
      ▼
执行修改
      │
      ▼
获取新数据
      │
      ▼
写入sys_operation_log
```

日志内容：

* 操作人
* IP地址
* 浏览器
* 操作时间
* 操作对象
* 修改前JSON
* 修改后JSON

日志不可修改。

---

# 6.13 数据一致性设计

为保证数据正确性，系统应满足以下一致性要求：

1. **唯一性约束**

   * 设备编号唯一；
   * SN号唯一；
   * 业务IP唯一；
   * 管理IP唯一。

2. **事务控制**

   * 新增设备时，设备主表、配置表、IP表、项目关联表等操作应置于同一数据库事务中，任一步骤失败均整体回滚。
   * 生命周期变更、维修登记等涉及多表更新的操作，应保证事务一致性。

3. **级联规则**

   * 删除设备采用逻辑删除，不物理删除历史数据；
   * 删除项目时，应先解除与设备的关联关系；
   * 删除部门前，应确保部门下无设备或完成设备迁移。

4. **统计同步**

   * 首页统计数据应基于业务数据实时计算，避免人工维护统计值。
   * 对于耗时较长的统计分析，可采用缓存机制提高查询性能。

---

# 6.14 异常数据处理流程

系统应对常见异常情况进行统一处理，确保数据安全和用户体验。

| 异常场景      | 系统处理方式                     |
| --------- | -------------------------- |
| 设备编号重复    | 阻止保存，提示用户修改                |
| SN号重复     | 阻止保存，提示冲突信息                |
| IP地址重复    | 拒绝保存，并定位冲突设备               |
| Excel模板错误 | 中止导入，返回模板错误信息              |
| 必填字段缺失    | 高亮提示缺失字段，不允许提交             |
| 生命周期非法流转  | 拒绝状态变更，记录异常日志              |
| 附件上传失败    | 回滚附件记录，提示重新上传              |
| 数据库事务异常   | 自动回滚事务，记录错误日志              |
| 权限不足      | 返回 403 Forbidden，不执行任何业务操作 |

---

## 6.15 本章总结

本章从登录认证、设备档案、生命周期、维修管理、项目关联、附件管理、Excel 导入导出、统计分析及日志审计等方面，对系统的数据流进行了统一设计。整个系统以**设备档案**为核心数据对象，通过统一的数据流和事务控制实现各业务模块之间的协同工作，并结合操作日志和生命周期日志实现全过程留痕，为后续数据库设计、接口设计和系统实现提供了明确的数据处理依据。
