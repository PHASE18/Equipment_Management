# 第五章 权限设计

## 5.1 权限设计概述

### 5.1.1 设计目标

为保障设备信息安全，规范系统使用行为，防止未经授权的数据访问和操作，本系统采用**基于角色的访问控制（Role-Based Access Control，RBAC）**模型，并结合**部门数据权限（Data Permission）**实现细粒度权限控制。

权限体系遵循以下原则：

1. **最小权限原则**：用户仅拥有完成本职工作所需的最低权限。
2. **职责分离原则**：系统管理、部门管理、设备维护等职责相互独立。
3. **数据隔离原则**：不同部门之间数据互不可见，避免越权访问。
4. **权限可扩展原则**：支持后续增加新的角色、菜单、按钮及数据权限策略。
5. **前后端双重校验原则**：前端负责界面控制，后端负责最终权限验证，防止绕过前端直接调用接口。

---

# 5.2 权限模型设计

系统采用 RBAC 权限模型。

用户、角色、权限之间关系如下：

```text
              用户(User)
                   │
          一个用户拥有多个角色
                   │
                   ▼
              角色(Role)
                   │
         一个角色拥有多个权限
                   │
                   ▼
             权限(Permission)
                   │
         包括菜单、按钮、接口权限
```

数据库关系如下：

```text
sys_user
    │
    ├──── user_role
    │
sys_role
    │
    ├──── role_permission
    │
sys_permission
```

系统登录后，根据用户拥有的角色自动加载对应菜单及操作权限。

---

# 5.3 系统角色设计

根据目前业务需求，系统共设置三类角色。

## 5.3.1 系统管理员（Administrator）

系统管理员拥有最高权限。

主要职责：

* 用户管理
* 部门管理
* 角色管理
* 权限管理
* 品牌管理
* 设备类型管理
* 供应商管理
* 项目管理
* 全部设备管理
* 全部日志查看
* Excel导入导出
* 系统参数配置

系统管理员可查看所有部门数据，不受部门限制。

---

## 5.3.2 部门管理员（Department Manager）

部门管理员负责本部门设备管理。

主要职责：

* 查看本部门全部设备
* 新增设备
* 修改设备
* 删除设备（逻辑删除）
* 新增维修记录
* 上传附件
* 查看统计分析
* 导出本部门设备信息
* 查看本部门日志

不得查看其他部门数据。

---

## 5.3.3 普通用户（Device Owner）

普通用户即设备责任人。

主要职责：

* 查看本人负责设备
* 修改本人负责设备部分信息
* 新增维修记录
* 上传维修附件
* 查看设备历史
* 查看本人统计信息

不得：

* 删除设备
* 查看其他部门设备
* 修改系统基础数据
* 查看系统日志

---

# 5.4 数据权限设计

本系统除功能权限外，还需要进行数据权限控制。

数据权限分三级。

## 一级：全部数据

适用于系统管理员。

可查看：

所有部门

所有设备

所有项目

所有日志

---

## 二级：部门数据

适用于部门管理员。

只能查看：

```text
本部门

↓

本部门设备

↓

本部门统计

↓

本部门日志
```

SQL示例：

```sql
WHERE department_id = 当前用户部门
```

---

## 三级：个人数据

适用于普通用户。

仅查看：

本人负责设备。

SQL示例：

```sql
WHERE manager_user_id = 当前登录用户ID
```

---

# 5.5 菜单权限设计

系统菜单采用树形结构。

建议如下：

```text
设备管理系统

├── 首页
│
├── 设备管理
│      ├──设备列表
│      ├──新增设备
│      ├──生命周期
│      ├──维修记录
│      ├──附件
│
├── 项目管理
│
├── 统计分析
│
├── Excel导入导出
│
├── 系统管理
│      ├──用户
│      ├──角色
│      ├──部门
│      ├──品牌
│      ├──设备类型
│      ├──供应商
│      ├──维保单位
│      ├──故障类型
│
└── 日志中心
       ├──登录日志
       ├──操作日志
       └──生命周期日志
```

菜单根据角色动态生成。

普通用户不会加载无权限菜单。

---

# 5.6 按钮权限设计

按钮权限比菜单权限更加细粒度。

例如：

设备列表页面：

```text
新增

修改

删除

导入

导出

上传附件

下载附件

查看日志

维修登记
```

不同角色对应按钮权限如下：

| 按钮      | 管理员 |  部门管理员 |    普通用户    |
| ------- | :-: | :----: | :--------: |
| 新增设备    |  √  |    √   |      ×     |
| 修改设备    |  √  |    √   | √（仅本人负责设备） |
| 删除设备    |  √  | √（本部门） |      ×     |
| Excel导入 |  √  |    √   |      ×     |
| Excel导出 |  √  |    √   |   √（本人数据）  |
| 上传附件    |  √  |    √   |      √     |
| 删除附件    |  √  |    √   |      ×     |
| 新增维修    |  √  |    √   |      √     |
| 修改维修    |  √  |    √   |   √（本人创建）  |
| 删除维修    |  √  |    √   |      ×     |
| 查看日志    |  √  | √（本部门） |      ×     |

---

# 5.7 接口权限设计

所有接口均采用权限校验。

例如：

| 接口                       | 权限编码               |
| ------------------------ | ------------------ |
| GET /device/list         | device:list        |
| POST /device             | device:add         |
| PUT /device              | device:update      |
| DELETE /device/{id}      | device:delete      |
| POST /device/import      | device:import      |
| GET /device/export       | device:export      |
| POST /maintenance        | maintenance:add    |
| DELETE /maintenance/{id} | maintenance:delete |
| GET /statistics          | statistics:view    |
| GET /log/list            | log:view           |

Spring Security 示例：

```java
@PreAuthorize("hasAuthority('device:add')")
```

接口权限与菜单权限保持一致。

---

# 5.8 数据权限实现

系统采用 **"角色权限 + SQL数据过滤"** 两层控制。

查询设备时：

管理员：

```sql
SELECT * FROM device;
```

部门管理员：

```sql
SELECT *
FROM device
WHERE department_id = 当前用户部门ID;
```

普通用户：

```sql
SELECT *
FROM device
WHERE manager_user_id = 当前登录用户ID;
```

统计分析模块同样按照数据权限过滤。

---

# 5.9 Excel 导入权限

Excel 导入属于高风险操作，仅允许：

| 角色    |    是否允许   |
| ----- | :-------: |
| 系统管理员 |     √     |
| 部门管理员 | √（仅本部门数据） |
| 普通用户  |     ×     |

导入时系统自动校验：

* 设备编号是否重复；
* SN号是否重复；
* IP地址是否冲突；
* 部门是否合法；
* 项目是否存在；
* 品牌是否存在；
* 设备类型是否存在。

导入完成后自动生成导入日志。

---

# 5.10 附件权限设计

附件遵循设备权限。

例如：

普通用户：

可以：

上传本人设备附件

下载本人设备附件

不能：

删除其他人附件

部门管理员：

可以：

删除本部门附件

管理员：

拥有全部附件管理权限。

---

# 5.11 日志权限设计

日志分三级。

登录日志

记录：

登录

退出

失败登录

操作日志

记录：

新增

修改

删除

导入

导出

生命周期日志

记录：

状态变化

所有日志均不得修改、删除（普通业务操作层面），仅系统管理员可根据保留策略进行归档或清理。

---

# 5.12 权限矩阵

| 功能模块    | 系统管理员 |  部门管理员 |   普通用户  |
| ------- | :---: | :----: | :-----: |
| 登录系统    |   √   |    √   |    √    |
| 首页统计    |   √   |    √   | √（本人数据） |
| 查看设备    |   全部  |   本部门  |   本人负责  |
| 新增设备    |   √   |    √   |    ×    |
| 修改设备    |   √   |    √   | √（本人负责） |
| 删除设备    |   √   | √（本部门） |    ×    |
| 生命周期管理  |   √   |    √   |    查看   |
| 维修管理    |   √   |    √   |    √    |
| 附件管理    |   √   |    √   |    √    |
| Excel导入 |   √   | √（本部门） |    ×    |
| Excel导出 |   √   |    √   | √（本人数据） |
| 项目管理    |   √   |    √   |    查看   |
| 统计分析    |   全部  |   本部门  |   本人数据  |
| 用户管理    |   √   |    ×   |    ×    |
| 部门管理    |   √   |    ×   |    ×    |
| 角色管理    |   √   |    ×   |    ×    |
| 品牌/类型管理 |   √   |    ×   |    ×    |
| 查看日志    |   全部  |   本部门  |    ×    |
| 系统配置    |   √   |    ×   |    ×    |

---

# 5.13 安全控制要求

为确保权限体系安全可靠，系统应满足以下要求：

1. 所有业务接口必须经过身份认证和权限校验。
2. 用户退出登录后，JWT Token 应立即失效或加入黑名单（如采用 Redis 管理会话）。
3. 用户不得通过修改 URL、请求参数或接口地址绕过权限控制。
4. 前端菜单和按钮仅作为展示控制，所有权限判断必须以后端校验结果为准。
5. 所有涉及新增、修改、删除、导入、导出等关键操作均需记录操作日志，内容包括操作人、时间、IP 地址、操作对象、修改前后数据及执行结果，确保系统具备完整的审计追踪能力。
